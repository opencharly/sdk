# 2026.233.1703 — Use org-wide reusable pr-validator architecture

Replaces the inline pr-validator with the dispatcher calling the
reusable workflow in opencharly/.github. Adds the approve-validator-runs
workflow for automatic approval of GITHUB_TOKEN-created runs. Removes
the old auto-merge.yml.

Change class: CI/workflow — architecture adoption.
