# 2026.202.1944 — clean --deep wire types (sdk leg)

Finalized by the fresh `pr-validator` at merge.

## What this leg is

The sdk half of the `charly clean --deep` cutover (the paired superproject leg
adds the plugin flag + the shared core purge engine): a SOLO-URGENT gap-closing
cutover adding a store-wide untagged/dangling-image purge to `charly clean`,
motivated by a real host holding 3,552 images / 132.6GB / ~97% reclaimable,
confounding build-corruption diagnosis (issue #173) and wasting the host.

### `sdk/schema/clean.cue` → `spec.RetentionRequest` / `spec.RetentionReply` (CUE-SOURCED, SDD)

**Round-2 correction (same cutover, pr-validator finding):** the hand-written
`sdk/spec/clean_wire.go` this leg originally shipped violated the "wire
types are CUE-sourced without exception" mandate — both types are PLAIN
structs `cue exp gengotypes` generates faithfully, no disjunction/
inexpressibility escape applies. Converted: `sdk/schema/clean.cue` is now
the single source (`#RetentionRequest`/`#RetentionReply`), `task cue:gen`
regenerates `spec/cue_types_gen.go` with byte-for-byte matching field
names/types/json tags (verified: `Dir string json:"dir"`, `Keep int
json:"keep,omitempty"`, `DeepBytes int64 json:"deep_bytes,omitempty"`, …),
and the hand-written `clean_wire.go` is DELETED — zero consumer changes
needed (`charly/host_build_retention.go` + `candy/plugin-clean/command.go`
already reference `spec.RetentionRequest`/`spec.RetentionReply` by name).
`buildctx.cue`/`clean.cue` are now both cited as LANDED conversions in
`/charly-internals:go`'s generation-coverage list (R5 sweep).

`RetentionRequest` gains `Deep bool` (the `--deep` request flag);
`RetentionReply` gains `DeepIDs []string` + `DeepBytes int64` (the removed/
would-remove image IDs and their summed reported storage size). **R1
correction (same cutover):** the orchestrator's destructive R10 run showed
`DeepBytes` heavily overstates actual freed disk (a live purge removing 68
untagged images reported ~92.6 GiB via this sum but freed only ~4.6 GiB real
disk) — most of the summed bytes are layers SHARED with images that remain.
`DeepBytes`'s CUE doc comment now states this plainly: it is an UPPER BOUND,
not a prediction, and pairing `--deep` with `--invalidate` gets closer to it.

### `sdk/kit/local_image.go`

`LocalImageInfo` gains a `Size int64` field, populated from the engine's
reported `"Size"` JSON field (bytes) in `ParseLocalImagesJSON` — needed to
report `--deep --dry-run`'s "reclaimable bytes" figure.

### `sdk/kit/http_fetch.go`

`humanBytes` exported to `HumanBytes` (its two existing download-progress call
sites updated in place) so `candy/plugin-clean` can format the reclaimable-
bytes report without duplicating the formatter (R3).

## Verification

`GOWORK=off go build/vet/test ./...` clean across the module, including
`TestGenReproducible` (a fresh `task cue:gen` off the committed `clean.cue`
reproduces the committed `spec/cue_types_gen.go` byte-for-byte — no drift);
`golangci-lint run` 0 issues (clean cache); `gofmt -l` empty; `go mod tidy` /
`go mod verify` no-op.

This leg is producer-only (wire additions, no behavior of its own), so its
R10 gate is the sdk standalone battery above PLUS the consumer leg's live
gate, run by the orchestrator on the `charly` `feat/clean-deep` branch this
leg feeds and pasted here as the chain's evidence (the paired superproject
CHANGELOG entry carries the full narrative):

```
$ charly clean --deep --dry-run    (consumer branch binary, real store)
deep: would remove 46 untagged image(s) store-wide (up to 76.5 GiB reclaimable
— shared layers may reduce actual reclaim; pair with --invalidate for the
fullest reclaim)  EXIT=0

$ charly clean --deep              (destructive, orchestrator-run)
3552→3484 images, 132.6GB→128GB   EXIT_CODE=0

charly check run check-commands-local: PASS (steps=6, run-calver
2026.202.1916) — includes the NEW clean-deep-dry-run-runs check step
EXIT_CODE=0
```

The changed wire fields (`RetentionRequest.Deep`, `RetentionReply.DeepIDs`/
`DeepBytes`) executed live end-to-end via this consumer chain — both the
destructive purge and the labeled dry-run drove them, and the bed's new
check step asserts the CLI surface this leg's types carry.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)
