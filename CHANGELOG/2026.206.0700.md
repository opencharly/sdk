# 2026.206.0700 — Cutover 1 sdk contract leg: build / check / deploy / credential-enc cone extensions + banked K1 loader Materializer seam

The sdk producer leg of the superproject's **Cutover 1** ("plugins-for-everything,
part 1 — all cones' UNBLOCKED capability moves out of `charly/` core **+ banked
reduction**"). One coherent sdk contract change: every wire/helper/schema
extension the cutover-1 charly-side plugin moves consume, plus the banked
prior-wave loader/enc reductions those moves ride on. The charly consumer leg
repoints its gitlink to the merged commit. **Land + tag first (B2 producer-first).**

Full diff: 29 files, +1602 / −408. Every theme below is part of it.

## Deploy cone
- **`#DeployTraits.bracketed_lifecycle` (new field, `schema/deploy.cue` → `spec`)** —
  a substrate declares whether its Start/Stop lifecycle needs the Q1 resource-arbiter
  bracket as DECLARED data (P9); `plugin-substrate` sets it true for `pod`, false
  elsewhere.
- **`ProvidedCapability → pb.DeployTraits` encode carries `bracketed_lifecycle`**
  (`schema.go`, `proto/plugin.proto`, `protocol/schema/plugin.cue`) — the field now
  survives the provider round-trip (in-proc AND grpc). Without it the field was
  silently dropped and `deployTraitsFor(word).BracketedLifecycle` was always false at
  runtime — the regression the `check-pod` bed caught in the consumer leg.
- **`AndroidEntityResolution` drops its google-play credential fields**
  (`schema/seam.cue`) — `candy/plugin-adb` resolves them peer-to-peer via
  `InvokeProvider(verb:credential)`.

## Build cone (relocated OUT of charly core into `sdk/buildkit`)
- `buildkit/privileged_runner.go` — `RunPrivileged`/`PrivilegedRun` unified (R3).
- `buildkit/init_config.go` — init-config + capability-aggregation + envd-removal.
- `buildkit/capabilities.go`, `buildkit/build_helpers.go`, `buildkit/status.go` —
  capability aggregation, pure build-engine helpers, candy status-rung helpers.
- **`#BuildResolveBox` extended** (`schema/buildresolve.cue`) for candy-side privileged
  bootstrap.
- `deploykit/graph.go` — `FilterBox` relocated (pure over the resolved box graph);
  `deploykit/deploy_host_helpers.go` sibling.

## Check cone (`sdk/kit`)
- `kit/apk_path.go` (`ResolveCommittedApk` — shared candy-origin apk anchor),
  `kit/checkvars_charlybin.go` (`StampCharlyBin`/`NewRuntimeCheckVarResolver` — shared
  `CHARLY_BIN` stamping + runtime check-var resolution), `kit/profile.go`,
  `kit/descent.go`.

## Credential / enc cone — wave-γ seam retirement (`schema/seam.cue`, `schema/settings.cue`, `deploykit/enc_probe.go`)
- **Retire the pod-config-status / mount / unmount / passwd seam wire types**, the
  **settings HostBuild seam wire types** (`schema/settings.cue` DELETED, −37), and the
  **enc-ensure / enc-unmount plan seam wire types** — the host no longer pre-resolves
  these behind a HostBuild seam.
- **Add seam-routable `deploykit.EncPlanForConfig` / `EncStatusFromConfig`** — the
  cutover-1 charly consumer's `candy/plugin-deploy-pod` + `candy/plugin-pod` build
  pod enc-ensure/unmount/tunnel/status plans themselves over these, instead of the
  retired host seam. This is the sdk half the credential/enc charly-cone move depends
  on directly.

## Banked K1 loader Materializer seam (`sdk/loaderkit`, `spec/loader_seam.go`)
- **`loaderkit.Materialize` + `spec.MaterializeSeams`** (K1 unit-1 kind-decode DISPATCH
  POLICY) — the per-node not-found policy ported out of charly's `normalizeNodeInto`;
  the registry-coupled decode stays host-side (clause M), the policy runs from the
  `spec.Threaded` recognition snapshot (clause D) + host callbacks. Banked from the
  `layer1-coremin-integration` wave that cutover-1's charly assembly rides on.
- **`spec.MaterializedProject` drops its dedicated VM/Pod/K8s/Local/Android fields**
  (the generic `Box`/`Candy`/`Bundle`/`PluginKinds` maps subsume them) — the
  regeneration driving most of the `spec/cue_types_gen.go` delta.
- `loaderkit/discover.go` seam sibling.
- **NEW test coverage:** `loaderkit/materialize_test.go` exercises all seven arms of
  the not-found dispatch policy (found / decode-error / deploy-substrate / kind-defer /
  kind-connect-error / kind-unreached / unrecognized) — closing the prior zero-unit-test
  gap on this banked seam.

## Verification
Standalone battery (`GOWORK=off`): `go build ./...` ✓, `go vet` ✓, `gofmt -l` clean
on changed files ✓, `go mod tidy` no-change ✓, `go mod verify` ✓, `task wire:gen` +
`task cue:gen` reproducible no-op ✓, `go test ./...` all packages green (a transient
`kit` subprocess-race timeout was load-starvation from a concurrent bed roster; passes
0.02s isolated). No `#SchemaVersion` bump — every change is a WIRE / helper / seam
contract change, not an authored-config ingress schema change, so no migration entry.
