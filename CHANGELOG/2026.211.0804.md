# 2026.211.0804 — ResolveLifecycleDeployNodeViaSeam plugin-side helper (#55 Cone A unit 1, sdk leg)

The consumer leg of #55 Cone A unit 1. Adds `ResolveLifecycleDeployNodeViaSeam`
to `sdk/deploykit` — the plugin-side helper that resolves a pod-lifecycle deploy
node (by box/instance) via the existing `LoadBundleConfigViaSeam`, BYTE-IDENTICAL
to the retired charly-core `resolveLifecycleDeployNode` (DeployKey + `dc.Bundle[key]`
lookup + container/""→pod normalization + `{Target:pod}` fallback). The invoking
plugin (command:pod / command:cmd) calls it and threads the resolved node as the
new `spec.Pod*Request.Node` DATA field, so the host reverse leg stops re-reading
the per-host config.

## What lands (additive)

- `deploykit/load_bundle_config_seam.go`: new `ResolveLifecycleDeployNodeViaSeam`.
- `go.mod`/`go.sum`: `require github.com/opencharly/spec` bumped `v0.2026211.549`
  → `v0.2026211.754` (the node-field producer).

Purely additive — a new exported helper; nothing renamed/removed.

Producer-first B2: spec (merged `v0.2026211.754`) → this sdk leg → superproject
(charly host leg uses `req.Node`, `resolveLifecycleDeployNode` DELETED,
pod_lifecycle_verb.go off sdk/deploykit).
