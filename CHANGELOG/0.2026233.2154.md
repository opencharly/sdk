# 0.2026233.2154 — ci: roll out org-wide bot-token validation flow

## ci: roll out org-wide bot-token validation flow

Replaces the per-repo inline dispatcher with the thin org-wide shape
(shared logic lives in `opencharly/.github`):

- Job renamed `charly/pr-validator` → `validate` (branch protection
  check becomes `validate / validate` — already updated).
- `workflow_dispatch` trigger added for manual re-validation.
- `approve-validator-runs.yml` deleted — obsolete: the shared reusable
  now finalizes the CHANGELOG via the Contents API with the
  `charly-auto-merge` app token, so its synchronize runs never land in
  `action_required` (proven end-to-end in opencharly/.github, PRs
  #52–#54: fresh PRs merged fully automatically).

### Change class

Code (workflow) — CI gate; non-runtime standards.

### How tested

The `.github` repo already runs this exact flow: PR #54 (fresh PR,
stale placeholder) merged fully automatically — API rename (app
commits), synchronize run without approval, AI review, auto-merge.
This PR exercises the same flow in the sdk repo.

### Project-rulebook compliance

- **R2** — one coherent change.
- **R3** — shared implementation in `.github`; per-repo dispatcher is
  the thin consumer.
- **R5** — obsolete approver deleted.
- **R6** — no force-push.
- **R7** — live proof in `.github` (#54) + this PR's own processing.
- **R8** — workflow YAML accepted by GitHub.
- **R9** — N/A.
- **R10** — fresh PR acceptance in the sdk repo after this lands.

### Attribution

*Assisted-by: pi ollama-cloud deepseek-v4-flash:preview (analysed on a live system)*