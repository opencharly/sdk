# 2026.233.1205 — Add check_run trigger as primary auto-merge trigger

Adds `check_run: [completed]` as the primary trigger alongside the
existing `pull_request: [labeled]` backup. The check_run event fires
every time any check completes, so the engine is re-triggered naturally
until all checks are green.

Change class: CI/workflow — trigger configuration change.
