# 0.2026236.1549 — test(buildkit): rpm host cell renders repo setup (regression for check-fedora-vm)

## Summary

The rpm `phase.install.host` cell in `charly/charly.yml` now writes the `.repo` file (with `gpgkey=`), imports the key (`rpm --import {{.gpgkey}}`), and passes `--enable-repo=<name>` on the install line (charly#396). This test pins that render with a literal host-cell fixture, mirroring `TestRpmTemplateWithModules`' container-cell fixture. The charly-side presence check lives in `charly/format_config_test.go` (charly/ core must not import sdk).

## How tested

```
$ go test ./buildkit/ -run "TestRpmHostCellHandlesRepos|TestRpmTemplateWithModules" -count=1
ok  	github.com/opencharly/sdk/buildkit	0.003s

$ go test ./buildkit/ -count=1
ok  	github.com/opencharly/sdk/buildkit	0.004s
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | RCA performed: the check-fedora-vm `No match for argument: charly` was traced to the rpm host cell having no repo handling; this test locks the fix |
| R2 | No deferral |
| R3 | The fixture mirrors the charly.yml host cell (one vocabulary) |
| R4 | N/A — test-only |
| R5 | N/A |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Test output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | Tests ran on the committed tree at this head |

## Change classification

- **Change class**: test (regression pin for the rpm host cell repo render)
- **Verification gate**: the render test + the org validator
- **Attribution tier**: `fully tested and validated` — the changed test ran green on the final tree

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*