# 2026.198.0740 — restore the 4 live deploy-seam wire types #66 over-removed (main-compat regression fix)

`sdk#66` removed `#DeployAddRequest`/`#DeployAddReply`/`#DeployDelRequest`/
`#DeployDelReply` from `schema/seam.cue` as "now-dead" — but their deadness was
true only on the UNMERGED K4 consumer branch (where dispatch.go replaces the
deploy-add/deploy-del HostBuild seams). On charly MAIN the types are LIVE:
`candy/plugin-bundle/bundle_cmd.go:68,104` forwards through them and
`charly/host_build_deploy_add.go`/`host_build_deploy_del.go` serve the seams.
Result: sdk main ≥ #66 could not be consumed by charly main — every superproject
gitlink bump broke `candy/plugin-bundle` compilation (surfaced by charly PR #141's
repoint; RCA traced to the #66 diff).

- Restores the 4 defs verbatim (from their pre-removal text) + `task cue:gen`
  regeneration; `#ResolveStartDirectRequest`'s removal in #66 was correct
  (zero consumers on charly main) and stays removed.
- The types become genuinely dead when the K4 deploy-cone wave lands dispatch.go
  on charly main — THAT cutover removes them together with the seams it retires
  (R5, one change).
- Producer-removal lesson (for the validator playbook): an sdk symbol's liveness
  is checked against the CONSUMER's MAIN, never against a sibling unmerged branch.

Assisted-by: Claude Code Anthropic Claude Fable 5 (analysed on a live system)
