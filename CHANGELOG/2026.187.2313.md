# mcp reverse-legs + shared MCP-provides types (H part 2e)

Complete the endpoint-seam reverse-leg surface for the last check verb (mcp).

- `proto/plugin.proto`: `rpc ResolveImageLabel(ResolveImageLabelRequest{label}) returns
  (ResolveImageLabelReply{value,error})` on `CheckContextService` (regenerated stubs) — read one
  OCI label off the deployment-under-test's image. Class-generic (parameterized by label name),
  never per-verb. `kit.CheckContext` + `sdkCheckContext` gain `ResolveImageLabel`.
- `spec/mcp_provides.go` (new): `MCPProvideEntry` (the RESOLVED mcp_provide entry) + the pure
  `PodAwareMCPProvides` move here from charly — shared by charly's DEPLOY-time provides injection
  AND the out-of-process mcp check verb, so it crosses the module boundary once (R3). `GetName`/
  `GetSource` let it satisfy charly's Named interface structurally. Unit-tested.

No CUE-schema change.
