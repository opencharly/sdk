# 2026.193.1245 — command:status collect-seam wire types (P14a producer)

The second sdk producer piece of P14a (status subsystem → plugins), following the status + OCI
wire types (2026.193.1015 / #28). Adds the host↔plugin collect-seam envelope the compiled-in
`command:status` candy exchanges to reach the fleet collectors, CUE-sourced in `schema/status.cue`
(SDD — WIRE TYPES ARE CUE-SOURCED WITHOUT EXCEPTION).

- **`#StatusCollectInput{Box, Instance, All, Nested}`** — the CLI selectors: `box=""` → the
  all-substrate fleet view (Collector.All), a non-empty `box` → the single-deployment detail
  (Collector.Single); `all`/`nested` mirror the `--all`/`--nested` flags.
- **`#StatusCollectReply{Rows []DeploymentStatus}`** — the collected rows (already merged,
  nested-overlaid, and sorted), which the candy renders (table / json / detail) from
  `spec.DeploymentStatus`.

Locked by `spec/status_wire_test.go` (`TestStatusCollectRoundTrip`). Additive API — the CONSUMER
(the `command:status` candy + the collectors→OpStatus move + the core status-engine deletion) is
the P14a superproject half, which is a SINGLE atomic cutover sequenced after P11 (the pod collector
lands in candy/plugin-deploy-pod, which P11 builds out — a partial collector move would be
forbidden dual-mode). This producer piece lands early per B2 so the superproject half consumes a
merged sdk tag.
