# 2026.198.0718 — K6: status-fanout op + P14: `kit.ResolveShellImageRef` (sdk producer leg)

## K6: status-fanout op — the wire selector for the status_collector whole-file move

Adds `OpStatusCollectAll`, the operation selector `candy/plugin-substrate`'s new
`verb:status-fanout` capability serves. It carries the SAME
`spec.StatusSubstrateRequest`/`spec.StatusSubstrateReply` wire shape the
"status-substrate" HostBuild seam already used — no schema change, just a new
op constant for a new (class, word) target on an existing wire pair.

Also ports `TestHostPortFor_Bridge`/`TestHostPortFor_HostNetwork` from
`charly/status_test.go` into `sdk/enginekit/engine_test.go` — the sole test
coverage of an sdk-only `ContainerSnapshot` method that had been sitting in a
charly-core test file with zero core coupling.

Companion PR (charly): the whole-file move of `charly/status_collector.go`
(the "registry-boundary blocker" verdict reopened and reversed) into
`candy/plugin-substrate/status_flat.go`.

## P14: `kit.ResolveShellImageRef` — shared image-ref resolution for `charly box merge`'s move

Adds `kit.ResolveShellImageRef(registry, name, tag string) string` to
`sdk/kit/local_image.go` — the registry/name/tag → full image reference
resolution (falling back to the newest local CalVer via
`ResolveNewestLocalCalVer` when tag is empty), relocated from charly core's
`charly/shell.go` `resolveShellImageRef` so `candy/plugin-box`'s new `merge`
command (the companion charly P14 cutover) can call it directly. Every
existing core call site (bundle_add_cmd.go, config_image.go, ensure_image.go,
image.go, remote_image.go, pod_lifecycle_resolve.go, update_deploy_dispatch.go)
keeps calling `resolveShellImageRef` unchanged — it is now a thin delegate to
this function (R3 single source, matching the established
`kit.InspectLabels = kit.InspectImageLabels` pattern).

Purely additive: no deletions, no schema change, no new dependency (uses the
already-kit-owned `ResolveNewestLocalCalVer`/`ResolveLocalImageRef`).

Review round: `kit/local_image_test.go` gained 5-branch coverage for
`ResolveShellImageRef` (tag-empty+local-hit newest-CalVer, tag-empty+miss+registry,
tag-empty+miss+no-registry, tag-set+registry, tag-set+no-registry — the tag-set
cases additionally assert local storage is never consulted), stubbing via the
`ListLocalImages` testability var.

## Verification

`GOWORK=off go build ./...` / `go vet ./...` / `go test ./...` (all packages
pass, incl. the new `enginekit` and `kit` coverage) / `golangci-lint run ./...`
(0 issues) / `gofmt -l .` (clean) / `go mod verify` + `go mod tidy` (no-op) —
all green on the merged head.

## Downstream

Consumer (charly PR #141, `feat/k6-status-collector-plugin-substrate`) calls
`sdk.OpStatusCollectAll` and `kit.ResolveShellImageRef(registry, name, tag)` —
confirmed matching names/signatures. It repoints its `sdk` gitlink +
`charly/go.mod` require to this commit's tag once this merges (B2 order).

Assisted-by: Claude Code Anthropic Claude Sonnet 5 (analysed on a live system)
