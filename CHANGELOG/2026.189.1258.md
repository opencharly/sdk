# 2026.189.1258 — sdk/buildkit: ResolvedBox keystone + BuilderMap/AggregatedCandyCaps (P3-unit-4)

Moves the fully-resolved image config `ResolvedBox` (+ `SupportsTag`/`SupportsBuild`)
out of charly into sdk/buildkit, together with its embedded value types `BuilderMap`
(+ `BuilderFor`/`HasBuilder`/`AllBuilder`) and the pure `AggregatedCandyCaps` result
struct. All field types are sdk-resolvable: `DistroConfig`/`BuilderConfig` (buildkit),
`MergeConfig`/`DistroDef`/`ResolvedInit` (spec aliases). The candy-model walks that
POPULATE these (`AggregateCandyCapabilities`, the init/caps resolution) stay charly-
side; only the pure result types move. All methods exported → charly repoints via
zero-churn aliases. Depends only on sdk/spec. Third P3 sdk/buildkit unit.

*Assisted-by: Claude (fully tested and validated)*
