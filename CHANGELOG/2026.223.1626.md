# 2026.223.1018 — rename K8s* identifiers to Kubernetes* (k8s→kubernetes substrate)

`ResolveK8sEntityViaExecutor`→`ResolveKubernetesEntityViaExecutor`, the
`"k8s"`→`"kubernetes"` kind word in the loader kind→def table
(`loaderkit/cue_schema.go`) and `fleetCrossRefKeys`
(`deploykit/deploy_nodeform.go`), the `K8s*`→`Kubernetes*` vmshared aliases
(`K8sDeployConfig`→`KubernetesDeployConfig`), and the `"k8s"`→`"kubernetes"`
substrate words in tests. Consumes spec v0.2026223.1018 (the spec-side
`k8s:`→`kubernetes:` wire-key rename). Placeholder CalVer — the pr-validator
finalizes it at merge.
