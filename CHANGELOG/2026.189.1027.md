# 2026.189.1027 — sdk/buildkit: the pure Containerfile render library (P3-unit-1)

First unit of the P3 sdk/buildkit extraction (core-minimization: "core is the
kernel; every capability is a plugin/library"). Introduces `sdk/buildkit` — the pure
Containerfile render/compute machinery any build front-end can import — carrying the
format/builder TEMPLATE render surface moved verbatim out of charly's
`format_template.go`: `CacheMount` + `Shared/OwnedCacheMount` + `RenderCacheMounts`/
`RenderCacheMountsAuto`, the shared `TemplateFuncs` map, `RenderTemplate`, the
`NewInstallContext` constructor, and `ToStringSlice`/`ToMapSlice`. Depends only on
`sdk/spec` (the CUE-sourced wire types) + `sdk/kit` (shell quoting) — never on charly
core. Behavior-neutral (charly aliases it zero-churn); proven by the golden
`go test` suite + the check-builder-local bed (all 4 steps green on a fresh rebuild).

*Assisted-by: Claude (fully tested and validated)*
