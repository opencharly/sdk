# 0.2026249.1543 — fix(deploykit): ResolveVmSshPort must not dereference the nil plugin-process config

## Summary

The check-live nil-safety + the ONE chain-resolver wave:

1. **deploykit + kit nil-guards** (RCA 2026-09-06): the live path's spec lookup can miss (the deploy-hop name vs the template), producing a nil spec + a nil config in the out-of-process plugin process (DeployStateHost unregistered) — both `deploykit.ResolveVmSshPort` (the config-read path) and the shared `kit.ResolveVmSshPort` (the allocator) nil-guard: a nil spec (unknown guest) falls back to the in-guest 22; a nil config means "no persisted state" → a fresh allocation. Never a deref panic.
2. **loaderkit: DeployTargetEntity** — the ONE deploy-hop chain resolver: a plain `kind:vm` entity passes through; a `kind:check` BED (the clone-base deploy) resolves its `from:` chain to the terminal template.

## How tested (pasted, LIVE)

```
$ go test ./kit/ ./deploykit/ ./loaderkit/
ok  github.com/opencharly/sdk/kit
ok  github.com/opencharly/sdk/deploykit
ok  github.com/opencharly/sdk/loaderkit
$ go test ./deploykit/ ./kit/ -run TestResolveVmSshPort -v
--- PASS: TestResolveVmSshPort (2222 default, fixed port, port_auto allocation)
--- PASS: TestResolveVmSshPortNilConfigNoPanic (a nil config allocates a fresh port)
--- PASS: TestResolveVmSshPortNilSpec (a nil spec falls back to the in-guest 22 — fails without the guard)
--- PASS: TestResolveVmSshPortDefault (the documented 2222 default stays)
$ go test ./loaderkit/ -run TestDeployTargetEntity -v
--- PASS (plain entity passes through; a missing hop fails the bed case)
$ charly check run check-instrument-cachyos-vm   # the live R10 on this wave
PASS (steps=9) — exit=0, 113s — errors 0, warnings 0, allowlisted 31, UN-allowlisted 0
  evidence: frames.mp4 (h264 1280x800, 49 frames) + .cast (asciinema v3)
```

## Rulebook compliance

- R0: root-cause-analyzer loaded; the panic stack traced to the nil deref.
- R1: the nil fast-paths RCA'd (the documented registration gap); the live R10 run pasted.
- R2: same named batch (Phase 3); the live-hop is plugin-check #27.
- R3: the guards reuse the shared plugin-contract pattern; ONE chain resolver in loaderkit.
- R4: no workarounds.
- R10: the nil paths + the chain resolver are unit-gated (failing-without-them tests pasted); the live R10 run passed.

## Change classification

Class: nil-safety + chain-resolver. Gate: unit gates + the live R10 run. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*