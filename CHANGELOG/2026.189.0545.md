# 2026.189.0545 — remove per-repo PR template; inherit org .github default

Deletes this repo's `.github/pull_request_template.md` so it falls through to the
single org-wide source `opencharly/.github` (the canonical rich template). Part of
the R3-at-repo-level consolidation: the same template previously lived duplicated
across 10+ repos. The authority for PR content stays CLAUDE.md + the git-workflow /
pr-validator skills; the template is the GitHub-UI mirror, now single-sourced.

*Assisted-by: Claude (documentation reviewed)*
