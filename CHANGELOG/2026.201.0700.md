# 2026.201.0700 — DEPLOY-wave sdk leg: pod command-seam wire types + kit/deploykit relocations

Companion sdk-repo leg of the charly-repo DEPLOY wave, which ported the 11
pod-lifecycle CLI commands (`start`/`stop`/`restart`/`logs`/`remove`/`shell`/
`service`/`volume`/`cp`/`config`/`update`) into the new compiled-in candy
`candy/plugin-pod`.

## New wire types (`sdk/schema/seam.cue` → generated `sdk/spec`)

Eleven `#Pod*Request`/`#Pod*Reply` pairs (Start/Stop/Logs/Remove/Shell/
Service/Update/ConfigSetup/ConfigStatus/ConfigMount/ConfigUnmount/
ConfigPasswd/ConfigRemove) carrying the registry- or type-bound commands'
authored flags across their `HostBuild("pod-<word>")` seams — the host
reconstructs the original core orchestration struct by name and runs it
verbatim, mirroring `candy/plugin-bundle`'s established `deploy-add`/
`deploy-del` seam shape.

## Relocations (completing earlier waves' incomplete cutovers)

Four charly-core files turned out to duplicate logic an earlier wave had
already relocated to `sdk/kit`/`sdk/deploykit` but never swept: `StripURLScheme`,
`ResolveAgentForwarding` (+ its one missing test), and the `FilterOwnProvides`/
`AcceptedEnvSet`/`ResolveTemplate`/`PortMapFromMappings` family were already
present with full coverage — the charly-side duplicates are deleted in the
companion charly-repo commit, this leg carries the sdk-side completions
(the missing `TestResolveGPGAgentForward_ContainerHome`, and two genuinely
new relocations: the pod quadlet path helpers (`PodQuadletFilename`/
`SidecarQuadletFilename` + instance variants, `sdk/kit/quadlet_paths.go`)
and the pod service start/stop/restart bodies (`sdk/deploykit/pod_service.go`,
split-consumer between the resource arbiter and the pod-deploy lifecycle).

## R1 corrections

Two header comments asserted a completed migration that verified false by
grep: `sdk/deploykit/container_resolve.go` claimed charly core's remaining
callers "now import deploykit directly" — eight call sites across the CHECK
and android waves still use the bare core duplicates, corrected to state the
true current state and track the dedup sweep to the CHECK wave.
`sdk/kit/agent_forward.go` claimed a `kit_aliases.go` passthrough that never
existed — corrected.

## Verification

`GOWORK=off go build ./...` + `GOWORK=off go test ./...` (all packages) +
`golangci-lint run ./kit/... ./deploykit/... ./spec/...` (0 issues) +
`task cue:gen` (verified true no-op, zero drift) — all green. Exercised
live via the companion charly-repo leg's R10 bed roster (check-exampledeploy,
check-charly-vm, check-sidecar-pod, check-group — all PASS, zero warnings).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)
