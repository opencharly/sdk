# 2026.219.1128 — rename Bundle→Fleet across deploykit/loaderkit/kit

`BundleConfig`→`FleetConfig`, `BundleNode`→`FleetNode`, the `Bundle` map→`Fleet`,
`BundleWalkPreOrder/PostOrder`→`FleetWalk*`, `FindBundleNode`→`FindFleetNode`,
the `bundle_*` file renames, and the seam-op comment
(pod-config-load-bundle→pod-config-load-deploy). User-facing error now says
`charly fleet add`. The on-disk `deploy:` config key is unchanged. Placeholder
CalVer — the pr-validator finalizes it at merge.
