# 2026.233.1159 — Test auto-merge with check_run trigger

This PR tests that the check_run trigger fires the auto-merge engine
whenever any check completes on the PR head. The engine checks ALL
check runs — if all green, it finalizes this CHANGELOG, enables native
auto-merge, and exits. The merge happens via GitHub's branch protection.
