# 0.2026250.758 — fix(loaderkit): reject duplicate top-level keys within a config-stack layer (R1/B5 the validation contract)

## Summary

sdk 702 introduced loaderkit/config_stack.go (absent in 527): LoadUnified merges the raw config layers BEFORE the walk. mergeYamlMapping silently collapses duplicate top-level keys WITHIN a single document (the yaml.Node round-trip preserves them; the merge idx-map dedups last-wins), making the parse-level "duplicate top-level entity name" contract (parse.go:107, pinned by charly's TestCrossKindNameReuse_LoaderAcceptsAllKinds) unreachable — a validation regression that silently masks config authoring errors.

Fix: per-layer duplicate-top-level-key rejection in mergeConfigStackRaw (dupTopLevelKey), before the cross-layer merge; merging ACROSS layers (the config-stack precedence) is unaffected. The duplicate-name contract error message mirrors parse.go's wording.

## How tested (pasted, LIVE)

```
$ go build ./...   → EXIT 0
$ go vet ./...     → EXIT 0
$ go test -count=1 ./...  → 10 packages ok, 0 FAIL
```
RED proven (pre-fix): the 3-key dupDoc (version/redis/redis) silently merged to 2 keys (no error); the mirror seam test panicked past the merge (nil seam deref at load_unified.go:119 — the load flowed on because the merge accepted the dup); cross-layer precedence merged fine (the guard preserves it). New tests: TestCrossKindNameReuse_LoaderRejectsWithinDocument (the sdk-seam mirror of the charly contract — a repeated top-level name in ONE document fails at the merge with the duplicate-name message) + the config_stack dup cases.

## Rulebook compliance

- R0: AGENTS.md read; git-workflow discipline.
- R1: the merge-vs-parse ordering defect RCAd (config_stack absent in 527, dup-collapse in 702); fixed at the merge seam.
- R2: no deferred defect — the fix is this PR.
- R3: one validation contract (the duplicate-name guard); no second guard invented.
- R4: no workarounds; the silent dedup is REJECTED, not tolerated.
- R5: n/a — no legacy removed.
- R6: PR-only; no force-push; no direct main push.
- R7: the full gate suite + the RED proof pasted above.
- R8: N/A — no runtime service change.
- R9: N/A — CalVer pins unchanged in this PR.
- R10: the suite ran on the committed tree (10 packages ok); the RED→GREEN proof is executed evidence.

## Change classification

Class: validation fix (CC-2 wave prerequisite). Gate: the full test suite + the executed RED→GREEN proof. Attribution: **fully tested and validated**.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*