# 2026.215.1806 — refactor(sdk)!: #55 import-purity — spec re-exports, engine test suites, cloud-init provisioning fixes

The sdk consumer/re-export leg of the #55 program (fe0e1d2→head):

- **Re-exports (zero-churn for the 85 plugins)**: ProvidedCapability/StepContract/MatchAll
  (73ba854-line), then ResolvedBox/BundleConfig/DeployRecord/CandyRecord/StepRecord/
  LedgerSchemaVersion/CloudInitRuntimeParams aliased onto spec; BundleConfig's 3
  kit-dependent methods demoted to deploykit free functions (DeployedContainerNames/
  OccupiedHostPorts/GlobalEnvForImage — 8 call sites updated in candy/plugin-deploy-pod).
- **New engine coverage (test relocations from charly, beside the engines they prove)**:
  deploykit gains the first-ever direct CompileOpSteps/CompileServiceSteps unit suite
  (plan_compile_test.go, 12 tests) + enc/quadlet, preempt-effective, apk-format,
  effective-version, bed-persist-writer, deploy-state-writer, venue-builder, init-def-label,
  and render/bootstrap coverage; kit gains checkvars-expand, planvalidate, and the
  RunOne/RunPlan wrapper-semantics + ProbeNeverHang suites; loaderkit gains
  validate-ephemeral + bakeplugin-effective-version; buildkit gains the render suite.
- **cloud-init provisioning chain (R10-roster-driven, guest-journal-evidenced)**:
  the pacman package install is `-Sy --needed` (scoped refresh+install — rotated-mirror
  404s fixed without destabilizing the live guest; the -Syu experiments and their
  three failure layers live in the chain's commit messages) + a sshd try-restart guard
  (the openssh-upgrade "hostkeys confused" wedge).
- **fix(exec-adjacent)**: the Wait-monitor pipe-deadlock test fix rides the spec leg;
  sdk's own suites are green against it.
