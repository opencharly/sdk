# Cutover A — BUILD + CHECK + P13-KERNEL (sdk contract leg)

**This is a PLACEHOLDER CalVer** (`2026.201.1917`), written by the Cutover-A
integrator. The evaluating `pr-validator` renames this file to the merge-time
CalVer and rewrites this heading to match, and stamps the Go-module tag
`v0.<YYYYDDD>.<HHMM leading-zeros-stripped>` — never edit an already-merged
`CHANGELOG/<CalVer>.md` afterward.

## What this cutover is

The sdk-contract half of Cutover A — the superproject counterpart
(`opencharly/charly`, `CHANGELOG/2026.201.1855.md`) carries the full
narrative; this entry covers what landed in `github.com/opencharly/sdk`.

- **BUILD** (`feat/build-wave`, `16aae7e`): dissolved the `formatPhaseTemplate`/
  `builderPhaseTemplate` var-alias family — `sdk/buildkit.FormatPhaseTemplate`/
  `BuilderPhaseTemplate` become the direct call targets; `ExtractMetadata`
  relocated to `sdk/deploykit/read_labels.go` (its natural home — it
  constructs `deploykit.VolumeMount` and `deploykit` already owns the
  emit-side mirror `WriteLabels`).
- **CHECK** (stacked on `feat/build-wave`): `sdk/kit/step_deps.go`
  (`FirstUnmetDepStep`), `sdk/kit/tagexpr.go` (`ValidateTagExpr`), `sdk/kit`
  gains `ReportStepResultsCount`; the container/engine-resolve dedup test
  relocation (`TestResolveBoxName` → `sdk/kit/remote_ref_test.go`); and
  `sdk/kit/vm_domain_lock.go` (`BedVmDomains`/`AcquireVmDomainLock`,
  extracted from charly-core's bed-session family — the one genuinely pure
  pair out of the K5-blocked bed-session mechanism).
- **P13-KERNEL** (`feat/p13-kernel-sdk`, `ad4a4ee`): the deploy-dispatch
  kernel's wire-contract half of the hard cutover:
  - `spec/provider_carriers.go` — the provider carrier interfaces relocated
    from charly core (the deploy-target/step/builder Provider abstractions a
    plugin implements against, now sdk-owned so a plugin never imports
    charly).
  - `schema/seam.cue` (+341 lines) — the whole `#DeployTreeResolveRequest`/
    `#DeployNodeDispatchRequest`/`#DeployMembersRequest`/
    `#DeployDelResolveRequest`/`#DeployNodeDelDispatchRequest` family (the
    walk-port's CUE contract, landed AHEAD of its consumer per this
    project's forward-schema pattern), plus the step-4 direction-flip's
    `PodConfigSetupRequest`/`PodConfigRemoveRequest`/the ~16 narrow
    `pod-config-*` seam request/reply pairs, plus `#DeployDispatchRequest`/
    `Reply` retirement (the retired K4-C spike types) and `VenueDescriptor`'s
    dead `nested`/`Jump`/`Parent` fields + `NestedJumpDescriptor` removal.
  - `schema/candy.cue` (+7 lines) — `#CandyArtifact` gains an optional
    `register` hint (the k3s-server artifact-declaration-driven dispatch
    fix — replacing a hardcoded `"k3s-server"` name check with a
    declared-intent field).
  - `sdk/deploykit` fold-ins: `InjectSecretsIntoPlans` (`secret_declare.go`),
    `RetrieveCandyArtifacts` + helpers (`artifact_retrieve.go`, with the
    former `loadedReadiness()` host coupling now a `vmshared.ResolvedReadiness`
    parameter the caller threads through), `RenderNamingPattern`/
    `NewEphemeralID`/`SanitizeUnitName` (`ephemeral_id.go`, tests moved
    1:1 byte-diff-gated), `deploy_add_shared.go`/`deploy_host_helpers.go`
    bodies (the 4/5 sdk lift), and `HostContext` gaining a preresolved
    `ActiveInit` slot (step-3 fix 1 — replacing a hardcoded `systemd`
    assumption with an empirically-proven-correct venue-declared value).
  - `sdk/kit` gains the relocated VM SSH `ReverseRunner` + port resolver
    (previously charly-core-only, now portable).

Plus the external main advance (`2e2ad01`, sdk#79): the downloads-cache-mount
curl-23 fix in `deploykit/tasks_emit.go` — `TaskCacheMounts`/`EmitDownload`
now follow the task's stage user (`taskRunsAsRoot`), giving non-root stages
an uid/gid-owned `/tmp/downloads` cache instead of a shared root-owned one
that a prior root-stage download could poison for every non-root stage.

## Conflict resolution (the sdk-side integration merge)

1. `feat/build-wave` (sdk) → `feat/p13-kernel-sdk`: clean merge, no
   conflicts (disjoint file sets — BUILD/CHECK touched `buildkit`/`kit`/
   `deploykit` label+task+alias files, P13-KERNEL touched `spec`/`schema`/
   the deploy-dispatch `deploykit` fold-ins).
2. `origin/main` (`2e2ad01`) → the result: **add/add conflict** on
   `deploykit/tasks_emit_test.go` — the BUILD-wave's wholesale test-suite
   relocation from `charly/tasks_test.go` (448 lines) and `origin/main`'s new
   curl-23 test block (`TestTaskRunsAsRoot` + `TestEmitDownload_DownloadsCacheOwnership`,
   67 lines) both created this file independently from the same base
   (`b0627c4`). Combined both — kept every BUILD-wave test, added the two
   new ones, and fixed `TestEmitDownload_TarGz`'s now-stale assertion (its
   fixture is a non-root UID-1000 stage, so post-fix it gets the uid-scoped
   `charly-tmp-downloads-uid1000` cache mount, not the pre-fix shared-form
   assertion). `deploykit/tasks_emit.go` itself merged clean (neither
   BUILD-wave nor P13-KERNEL touched it — only tests moved).

## Verification

- `GOWORK=off go build ./...` / `go vet ./...`: clean, standalone.
- `GOWORK=off go test ./... -count=1`: green — all 16 packages (`sdk`,
  `agentkit`, `buildkit`, `deploykit`, `enginekit`, `internal/wiregen`,
  `kit` ~10.4s, `loaderkit`, `spec`, `targetkit`, `vmshared`; the rest carry
  no test files).
- `golangci-lint run ./...`: **0 issues**.
- `TestGenReproducible`: passes as part of the standard suite (regeneration
  is a no-op on this tree — `task cue:gen` confirmed no-drift on the
  superproject side, which chains this module's generation first).

## Consumer-side live proof (the superproject's R10, run against this sdk
commit via the in-tree `replace` resolution)

5/5 GREEN, all concurrent, zero warnings, run by the orchestrator against
the assembled superproject (charly `efdc5a3`/`dd6801b` + this sdk commit):
`check-exampledeploy` 145s, `check-substrate` 240s, `check-charly-vm` 338s,
`check-group` 407s, `check-sidecar-pod` 456s — every summary `ok:true`,
exit 0. This is the schema+wire-type contract's real proof: the walk-port's
`#DeployTreeResolveRequest`/`#DeployNodeDispatchRequest` family and the
step-4 `pod-config-*` seam types round-trip correctly through the whole
bundle add/del control flow and the nested-tree ancestor-chain
reconstruction (`check-group`/`check-sidecar-pod`), and the VM
`node_only`/`RootVenueSSH` short-circuit path (`check-charly-vm`) exercises
the ONE schema gap this wave found + fixed (`#DeployNodeDispatchRequest`
was missing `node_only`).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)
