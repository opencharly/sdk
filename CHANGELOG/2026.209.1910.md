# 2026.209.1910 — #118 core-minimization sdk PRODUCER leg (breaking): mechanism kits + wire contract for the plugin-host end-state

The sdk half of the **"everything is a plugin" core-minimization program** (`charly#118`),
bundling ~30 cone relocations into one producer leg (B2 first; the superproject consumer
leg — charly RESIDUE→0 + the `candy/plugin-*` consumers, gitlink → this merged commit —
follows). **95 files, +7,211 / −1,104.** Contains sdk `origin/main` (up to date, no rebase).

Marked `!` (breaking) because it retires the transitional per-capability wire contracts the
old in-core seams used, now that the capabilities resolve into GENERIC envelopes instead.

## Mechanism kits relocated in (the M-mechanisms the plugins consume)

- **loaderkit** (19 files) — the kind-blind loader made plugin-callable: `LoadUnified` +
  materialize orchestration (K1), and `ResolveResource/Android/VmViaExecutor` — the mid-load
  `InvokeProvider(kind, OpResolve)` callbacks the 3 substrate plugins self-serve for
  ValidatePreemptible / ValidateAndroidDevices (replacing the host loader-validate legs).
- **deploykit** (35 files) — the deploy/build mechanism kit the plugins drive: the
  Primaries-driven `MarshalBundleNode` (config-corruption pod-fixes converged), the
  venue-agnostic BuilderStep orchestration + venue-home-artifact builder, candy
  `secret_requires:`/`secret_accepts:` resolution, `pruneContainerInitForSystemd`, and the
  Containerfile render DRIVE (#67, driven by plugin-build over the resolved-project envelope).
- **buildkit** (4) — remote-build-ref + `ResolvedBox` view helpers.
- **vmshared / sshx / kit** (7+1+6) — shared host-object helpers hoisted for one-copy reuse
  (R3): libvirt user-session, SSH keypair, file-lock + `DefaultDeployConfigPath`, distro
  detection. (`vmshared/readiness_methods.go` folded into its sibling.)

## Breaking wire-contract removals (R5 — retired identifiers, recorded here)

Retired because the capability now resolves into a generic envelope a mechanism consumes,
not a per-capability HostBuild round-trip:
- **`#PodConfig*` family** (schema/proto): `#PodConfigResolveSidecars{Request,Reply}`,
  `#PodConfigProvisionSecrets{Request,Reply}`, `#PodConfigEncMounts{Request,Reply}`,
  `#PodConfigInjectEnvProvides{Request,Reply}`, `#PodConfigInjectMCPProvides{Request,Reply}`,
  `#PodConfigHookSecretEnv{Request,Reply}` — the pod-config provides/secrets/enc injection
  moved plugin-side into candy/plugin-deploy-pod (P11).
- **`#EnsureImage{Request,Reply}`**, **`#DeployTreeResolve{Request,Reply}`**,
  **`#DeployNodeDispatch{Request,Reply}`** — the deploy-dispatch kernel's per-node seams
  retired into candy/plugin-bundle's own walk (K4).
- **`ResolveClusterContext` RPC** (+ its Request/Reply proto messages) — k8s cluster-context
  resolution moved plugin-side via `exec.InvokeProvider`.
- `ConfigResolveReply.Backend` / `EmitReply.ActScript` shape narrowing on the surviving ops.

Code sweep clean: the retired identifiers appear only here + migration history (R5 grep self-test).

## SDD

Wire types are CUE-sourced (`schema/*.cue` → `task cue:gen`); no hand-written authored wire
struct, no unjustified `@go(-)`. The `#DeployCompileRequest` discriminated shape,
`#ResolvedProject.Primaries`, and `#PodCmdRequest/#PodCmdReply` (the pod-cmd host-builder wire)
are the new authored surfaces. `TestGenReproducible` GREEN — schema regen a no-op.

## Verification

`GOWORK=off go build ./...` / `go vet ./...` clean; full `go test ./...` GREEN across every
sdk package; `TestGenReproducible` GREEN. No `github.com/opencharly/charly` import (contract
module). The sdk contract-module gate IS the standalone battery above; the RUNTIME proof
of the consuming plugins + superproject core belongs to the consumer leg — the full per-bed
terminal R10 roster (fresh-rebuild, with verbatim on-disk `.check/<bed>/<calver>/summary.yml`
references) is pasted in the superproject consumer PR, not duplicated here.

Assisted-by: Claude Code Anthropic Claude Opus 4.8 (fully tested and validated)
