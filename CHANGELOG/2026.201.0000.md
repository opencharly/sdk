# 2026.201.0000 — fix stale bed-name reference in candy_steps.go comment

`WriteCandySteps`'s localpkg hard-error comment cited "the
check-charly-selftest-pod defect" as the motivating incident for
fail-loudly-never-silent-fallback. That bed name has never resolved to
anything in either repo's history — the box/bed that surfaced this exact
failure mode (a `--dev-local-pkg` build shipping an image with no
`/usr/bin/charly`) is `check-charly-vm`, the toolchain localpkg deploy
witness. Corrected the comment to name the bed that actually exists.

Companion charly-repo leg: opencharly/charly's thematic hygiene batch
(sdk-interface-drift fixture fixes + dead-code sweep), which found this
stale reference while sweeping for the same bed name across both repos.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; documentation reviewed)
