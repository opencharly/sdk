# 2026.195.1830 — Keep remote bases out of the local box graph

- Add box dependency edges only when the referenced base is a node in the local
  graph, preventing both empty `from:` bases and namespaced remote bases from
  producing false empty-cycle validation errors.
- Cover from-only and namespaced-remote-base box order with regression tests.
