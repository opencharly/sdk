# 2026.195.1830 — Ignore empty `from:` base graph edges

- Prevent resolved `from:` boxes with no `base:` from creating a phantom empty
  dependency and a false empty-cycle validation error.
- Cover the from-only box order with a regression test.
