# 2026.189.1132 — sdk/buildkit: distro/builder vocabulary resolver (P3-unit-2)

Moves the embedded build-vocabulary resolved-config types + their resolution
methods out of charly into sdk/buildkit: `DistroConfig`/`BuilderConfig` +
`ResolveDistro`/`ResolveInherits`/`FindFormat`/`AllFormatNames`/`ValidFormat`/
`ExpandPackageInheritance`/`ValidBuilderType`/`BuilderNames` + `WrapDistroDef`/
`DistroTagChain` (exported so charly can bind them). Depends only on sdk/spec.
The Phase/Venue-coupled phase-template resolvers + loader glue stay charly-side.
Behavior-neutral; proven by charly's golden distro_cascade/format_config tests +
the check-builder-local bed (4/4 green, fresh rebuild). Second P3 sdk/buildkit unit.

*Assisted-by: Claude (fully tested and validated)*
