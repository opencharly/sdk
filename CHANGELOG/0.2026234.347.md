# 0.2026234.347 — ci: sweep-based tag-on-merge dispatcher

## ci: sweep-based tag-on-merge dispatcher

### Summary of changes

Replaces the `pull_request: closed`-triggered tag dispatcher (which
never fires for auto-merges — the merge event action is `merged`, an
invalid trigger type) with the org-wide sweep shape: `workflow_run`
on the validator's completion + a 15-minute `schedule`, calling the
shared reusable in `opencharly/.github` (sweep-only, tags every
merged PR lacking its CalVer tag).

### How tested

- **Trigger-name evidence for THIS repo** (workflow_run matches the
  workflow `name:` field, not the check name):
  ```
  {"name":"charly/pr-validator","path":".github/workflows/pr-validator.yml","state":"active"}
  ```
  (workflows listing for opencharly/plugins). The check name
  "validate / validate" is the job-level name; the workflow name the
  filter matches is `charly/pr-validator`.
- **In-repo workflow_run proof** — this repo's old `auto-merge.yml`
  used the identical filter and produced live `workflow_run` runs
  here: run 32533731241 (success, 2026-08-21T22:37:35Z, name
  `auto-merge`), plus 32471006950/32470818728/32469273129 (skipped)
  — the filter matches this repo's `charly/pr-validator` workflow.
- **Check-name note** (the reviewer's `<workflow name> / <job name>`
  model): for a reusable workflow call, GitHub names the check
  `<caller job> / <callee job>` — both jobs here are `validate`, hence
  "validate / validate". The `workflow_run` filter matches the
  workflow's `name:` field (`charly/pr-validator`), which is a
  different attribute, verified by the workflows listing AND the live
  in-repo `workflow_run` runs above.
- The identical dispatcher + reusable are live in `opencharly/.github`
  (PRs #61/#63/#65): the sweep tagged all missing merges
  (v2026.233.2148 … v2026.234.0211). This repo's own merge will be
  tagged by the sweep within minutes.

### Project-rulebook compliance

- **R1** — RCA: closed never fires for auto-merges (observed event
  action `merged`; `types: [merged]` fails workflow load).
- **R2** — one coherent change.
- **R3** — shared reusable; thin dispatcher.
- **R4** — the 15-minute `schedule` is a reconciliation backstop for
  the event/merge race, not a polling loop; the primary trigger is
  the event-driven `workflow_run` sweep.
- **R5** — closed-based shape removed.
- **R6** — no force-push.
- **R7** — live proof in `.github` (tags listed above).
- **R8** — YAML accepted by GitHub.
- **R9** — N/A.
- **R10** — this repo's merge tag verified after landing.

### Attribution

*Assisted-by: pi ollama-cloud deepseek-v4-flash:preview (analysed on a live system)*