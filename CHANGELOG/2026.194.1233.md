# 2026.194.1233 — kit: fix stale ValidateRecord seam comment (R1)

2026-07-13

The `ValidateRecord` egress-validation seam comment in `sdk/kit/deploy_types.go`
referenced a nonexistent `charly/install_ledger_egress.go` as the site where charly
injects its `ValidateEgressValue`. The injection actually lives in
`charly/kit_ledger_aliases.go`'s `init()`. Comment-only, no behavior change — an R1
documentation-divergence fix surfaced during the K4 deploy-resolution survey.
