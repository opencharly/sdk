# 2026.233.2152 — Roll out the org-wide bot-token validation flow
<!-- auto-merge-finalized: 2026.233.2152 -->

The dispatcher now matches the org-wide architecture: job `validate`
(check `validate / validate`), a `workflow_dispatch` trigger for
manual re-validation, and the rename handled by the shared reusable in
opencharly/.github (Contents API with the `charly-auto-merge` app
token — no approval-required runs). The obsolete
`approve-validator-runs.yml` is deleted.