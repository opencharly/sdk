# 2026.189.0625 — gofmt the 5 drifted hand-written spec wire files (SDD hygiene)

`gofmt -l .` flagged 5 hand-written files committed unformatted across earlier
cutovers (Cutover L/F/K/H): spec/{deploy_wire,init_wire,k8sgen_wire,vm_wire}.go +
spec/mcp_provides_test.go. Runs `gofmt -w` on them (the only sanctioned blanket
auto-fix — AST-identical, behavior-neutral). Not the cue-generated `*_gen.go` (so
`task cue:gen` is unaffected); a hand-authoring gofmt miss, an SDD/R1 hygiene
incident. After: `gofmt -l .` empty, `go build ./...` + `go test ./...` green in
sdk, and the in-tree charly consumer builds unchanged.

*Assisted-by: Claude (analysed on a live system)*
