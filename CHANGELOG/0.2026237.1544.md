# 0.2026237.1544 — fix: the sdk's cross-repo plugin deps advance to the standalone module paths (the candy cutover, Phase 2c ripple)

## Summary

The candy de-submodule cutover, Phase 2c ripple: the sdk module's two cross-repo plugin dependencies advance from the OLD in-repo paths to the standalone module paths. The sdk's go.mod required `github.com/opencharly/charly/candy/plugin-egress` + `plugin-init` (test-only imports in `deploykit/render_service_seam_roundtrip_test.go`); after the plugins moved to standalone repos (charly#408 + umbrella#26), those paths now resolve to the standalone modules `github.com/opencharly/plugin-egress/candy/plugin-egress` + `github.com/opencharly/plugin-init/candy/plugin-init`.

## How tested

**The affected test passes at the new module paths:**

```
$ GOWORK=off go test ./deploykit/ -run "TestRenderServiceSeam" -count=1
ok  ...
```

**go.mod diff (the two requires advance):**

```
-	github.com/opencharly/charly/candy/plugin-egress v0.0.0-20260806230842-5cda00cb0fbc
-	github.com/opencharly/charly/candy/plugin-init v0.0.0-20260806230842-5cda00cb0fbc
+	github.com/opencharly/plugin-egress/candy/plugin-egress v0.2026237.1419
+	github.com/opencharly/plugin-init/candy/plugin-init v0.2026237.1424
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — Go dependency advance |
| R1 | The stale in-repo requires were RCA'd (the sdk's go.mod pinned the old paths) and advanced |
| R2 | No deferral — the cross-repo requires land here |
| R3 | One module-path source (the standalone repo's go.mod) |
| R4 | N/A — immutable tags |
| R5 | The old in-repo paths are removed from the sdk's go.mod + test imports |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Test output pasted above |
| R10 | The changed paths ran live (the test resolves the standalone modules) |

## Change classification

- **Change class**: cross-repo Go dependency advance (Phase 2c ripple)
- **Verification gate**: the deploykit test + the org validator
- **Attribution tier**: `analysed on a live system` — the test resolved the standalone modules at their pinned tags

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*