# 2026.211.0606 — deploykit drops the deploy value-vocabulary (#55 Cone V, sdk leg)

The deploy CONFIG/KEY value-vocabulary moved to the `spec` contract module (Cone V
producer leg). This leg deletes the original definitions from `sdk/deploykit` and
adds thin re-export aliases so deploykit's own callers, its tests, and the deploy
candies keep referencing `deploykit.X` unchanged.

## What changes

- **`deploykit/deploy_key_aliases.go`** (new) — thin re-export aliases:
  `var DeployKey = spec.DeployKey`, `ParseDeployKey`, `BundleDelArgv`,
  `DeriveDeploymentName`; `type SaveDeployStateInput = spec.SaveDeployStateInput`,
  `type OCIEmitStepParams = spec.OCIEmitStepParams`.
- **Deletions** (the definitions now live in `spec`):
  - `deploykit/bundle_del_argv.go` (whole file — `BundleDelArgv`).
  - `deploykit/deploy_state.go` — `DeployKey`, `ParseDeployKey`, the
    `SaveDeployStateInput` struct (`SaveDeployState`, the persist FUNCTION, STAYS).
  - `deploykit/deploy_host_helpers.go` — `DeriveDeploymentName` + `lastIndexByteInRef`.
  - `deploykit/oci_target.go` — the `OCIEmitStepParams` struct (the `OCITarget`
    walker STAYS).
- **`go.mod`** — the `require github.com/opencharly/spec` version bumps to the Cone V
  producer tag `v0.2026211.549`.

Deploy BEHAVIOUR (`SaveDeployState`, the `OCITarget` walker, the step-emit dispatch)
is unchanged — the public `deploykit.X` surface is byte-equivalent (aliases resolve to
the same `spec` types). Validated STANDALONE with `GOWORK=off` against the merged spec
tag: `go build ./...` 0, `go vet ./...` 0, `go test ./...` ok (all packages),
`gofmt -l` empty, `go mod verify` OK. The two pre-existing `dupl` findings in
`deploykit/intermediates_move_test.go` are untouched by this leg (→ lint batch).

_R10: the assembled Cone V tree passed fresh-rebuild check-pod (13/13) + check-builder-vm
(10/10), exercising the deploy paths that consume this vocabulary._

_Assisted-by: Claude Code (Claude Opus 4.8) (fully tested and validated)_
