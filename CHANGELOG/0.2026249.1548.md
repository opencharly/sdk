# 0.2026249.1548 — test(vmshared): custom_commands threads from the answer map into the seed

## Summary

`TestRenderInstallerSeed_CustomCommands` (test-only): the answer map's `custom_commands` must thread into the rendered archinstall config (RCA: the omarchy template hardcoded `[]` — the authored answer.custom_commands never reached the seed). The guard renders the authored JSON array when present, `[]` when absent.

## How tested (pasted)

```
$ go test ./vmshared/ -run TestRenderInstallerSeed -v
--- PASS: TestRenderInstallerSeed_IsDeterministic
--- PASS: TestRenderInstallerSeed_NoValueSentinelIsRejected
--- PASS: TestRenderInstallerSeed_CustomCommands   # the absent-path branch now t.Fatalfs when custom_commands leaks through (fails loudly)
ok  github.com/opencharly/sdk/vmshared
$ gofmt -l vmshared/   # clean
```

## Rulebook compliance

- R0: root-cause-analyzer loaded; the hardcoded-`[]` gap traced to the answer map.
- R1: the absent-path assertion was vacuous (empty if body) — now fails loudly (B18).
- R2: same named batch (Phase 3).
- R3: no duplication; the guard is the single render path.
- R4: no workarounds.
- R10: the test is unit-gated + gofmt-clean.

## Change classification

Class: test-only (the custom_commands threading gate). Gate: the unit test. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*