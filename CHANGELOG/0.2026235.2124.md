# 0.2026235.2124 — fix(loaderkit): a root-level remote candy has no siblings — skip the bare-dep qualify

## Summary

The candy de-submodule cutover's **root-level sibling-qualify fix** (sdk-side): a standalone candy repo's manifest lives at the repo ROOT (`SubPathPrefix ""`) and has NO sibling candies. `QualifyRemoteSiblingDeps` previously fabricated a wrong sibling path for a bare dep (`github.com/org/layer-python/pixi` for a bare `pixi` dep), failing the remote resolution with "not found". The root-level remote now **skips the qualify** — the bare name resolves against the scan set (the local library or another downloaded remote). The candy-library form (`SubPathPrefix "candy/"`) is unchanged.

## How tested

**The new + regression tests (pasted):**

```
$ GOWORK=off go test ./loaderkit/ -run "TestQualifyRemoteSiblingDeps|TestScanRemoteCandy" -v
=== RUN   TestScanRemoteCandy_RootLevelNameDerivation
--- PASS: TestScanRemoteCandy_RootLevelNameDerivation (0.00s)
=== RUN   TestScanRemoteCandy_SubPathNameUnchanged
--- PASS: TestScanRemoteCandy_SubPathNameUnchanged (0.00s)
=== RUN   TestScanRemoteCandy_ForeignRefStillErrors
--- PASS: TestScanRemoteCandy_ForeignRefStillErrors (0.00s)
=== RUN   TestQualifyRemoteSiblingDeps_RootLevelSkips
--- PASS: TestQualifyRemoteSiblingDeps_RootLevelSkips (0.00s)
=== RUN   TestQualifyRemoteSiblingDeps_SubPathStillQualifies
--- PASS: TestQualifyRemoteSiblingDeps_SubPathStillQualifies (0.00s)
PASS
```

**The full loaderkit suite (pasted):**

```
$ GOWORK=off go test ./loaderkit/
ok  	github.com/opencharly/sdk/loaderkit	0.006s
```

**The live defect this fixes (pasted — the batch-1 check-marketplace bed, BEFORE):**

```
scanning github.com/opencharly/pod-chrome-cdp:v2026.235.2110: remote candy github.com/opencharly/pod-chrome-cdp/plugin-cdp not found
```

The bare `plugin-cdp` dep of the remote chrome-cdp candy was qualified to a fabricated sibling path; with this fix the bare name resolves against the scan set.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched (a pure mechanism kit) |
| R1 | the batch bed's remote resolution failed live — root-caused (the sibling-qualify fabricated a root-level sibling) + fixed in-tree |
| R2 | N/A — the surfaced failure was fixed, not parked |
| R3 | the ONE QualifyRemoteSiblingDeps owns the qualification; the sub-path regression test guards the pre-cutover form |
| R4 | N/A — no workaround |
| R5 | N/A — the sub-path form is preserved |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the test + suite outputs pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | the tests ran on the committed tree |

## Change classification

- **Change class**: sdk mechanism fix + tests
- **Verification gate**: the full loaderkit suite (pasted above)
- **Attribution tier**: fully tested and validated — the changed path executed live via the new tests + the full suite green
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*