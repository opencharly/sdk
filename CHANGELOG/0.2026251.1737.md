# 0.2026251.1737 — refactor(sdk): parser consolidation F4.3/F4.4/F4.5 — ParseRemoteRef, RepoLayoutDirs, stale docstring

## Summary

Cutover F parser consolidation, F4.3 + F4.4 + F4.5 (the sdk repo leg of the cross-cutting dedup wave).

**F4.3 — remote-ref parser ×2 → both call the canonical `spec.ParseRemoteRef`.**

Duplication evidence (current tree):
- `candywalk/candywalk.go:265` — hand-rolled `parseRemoteRef` + `parsedRemoteRef` (repo-path/version split of `@github.com/opencharly/...:v<calver>` refs)

Consolidation: `spec.ParseRemoteRef` (spec/ref_parse.go) performs the identical decomposition (first-3-segment repo path + version). The local copy is deleted; both call sites (the remote-fetch queue walk and `collectRefs`) call `spec.ParseRemoteRef` directly. The nil-reduced guard becomes an empty-`RepoPath` guard (ParseRemoteRef never returns nil).

**F4.4 — repo-layout constant held twice → one shared sdk constant.**

Duplication evidence:
- `candywalk/candywalk.go:139` — `[]string{"candy", "box"}` in the root walk
- `kit/migrate_helpers.go:117` — `MigrateCandidateYAMLFiles(dir, []string{"candy", "box"})`

Consolidation: one `kit.RepoLayoutDirs` constant; both sides read it.

**F4.5 — stale docstring** `kit/planvalidate.go:11-13` claimed callers in the DELETED `charly/validate.go` and `charly/host_build_feature.go`; restated against the actual consumers (externalized `charly box validate` / check/feature surfaces).

## How tested

```
$ cd sdk && gofmt -l . && go build ./... && go vet ./...
(empty, exit 0)
$ go test ./...
(no failures; candywalk + kit suites green)
```

## Rulebook compliance

- R3: one canonical remote-ref parser (spec.ParseRemoteRef) and one repo-layout constant (kit.RepoLayoutDirs); the dup copies deleted in the same change.
- R1: verified against the CURRENT tree (the candywalk dup was still live at candywalk.go:265 — the parent's "no longer present" note is stale; my grep re-verified it exists).

## Change classification

- Change class: refactor (parser consolidation; byte-identical behavior — spec.ParseRemoteRef has the same repo-path/version semantics)
- Verification gate: full `go test ./...` on the final tree
- Attribution tier: `analysed on a live system` — sdk test suite ran live on the changed tree.

*Assisted-by: pi anthropic/claude-sonnet-4-20250514 (analysed on a live system)*