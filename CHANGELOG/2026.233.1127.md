# 2026.233.1127 — Replace inline auto-merge with reusable engine from .github
<!-- auto-merge-finalized: 2026.233.1127 -->

Replaces the inline auto-merge engine with a dispatcher calling the
canonical reusable engine in opencharly/.github.  Adds tag-on-merge
dispatcher for CalVer tagging.  Removes old harness scripts.

Change class: CI/workflow — dispatcher + new tag workflow.

