# Distro resolve wire types (Cutover M, the long pole)

Support the distro de-type: candy/plugin-distro resolves an authored `distro:`
build-vocabulary entity into a ResolvedDistro the kernel's build engine consumes
without importing the concrete spec.Distro.

- `spec/distro_wire.go` (hand-written): `ResolvedDistro` (mirrors spec.Distro's
  fields + Raw, with the `PrimaryFormat`/`LocalPkgFormat` methods mirrored),
  `DistroResolveInput/Reply`.
- `vmshared/spec_aliases.go`: `DistroDef = spec.ResolvedDistro` (was
  `spec.DistroDef`) — this ONE alias flip moves every build-engine consumer (97
  references) to the resolve envelope; the host keeps RenderTemplate + the
  cache-mount vocab per the plan.

No CUE-schema change (the authored `#Distro` shape is untouched) — no `#SchemaVersion`
bump, no gen-repro drift.
