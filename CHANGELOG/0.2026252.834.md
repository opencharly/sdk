# 0.2026252.834 — feat(checkkit): the RunProvisionAct leg (the full kit.VerbResolver seam)

## Summary
Two changes in the checkkit package:
1. **RunProvisionAct leg** — the do:act state-provision verbs' act dispatch over the same single dial, completing the kit.VerbResolver seam so consumers (plugin-check, plugin-pipeline) use it directly with no wrapper. R3: RunVerb and RunProvisionAct now share ONE dispatch helper (the nil-executor guard, the op/env marshal, the InvokeProvider call, the result decode — the body exists exactly once).
2. **MCPProvide snapshot** — the CheckEnv snapshot now carries the runner's MCPProvide (the VM venue's mcp_provide declarations), so the wire envelope matches the plugin-local snapshot it replaces.

## How tested (pasted)
```
go build ./checkkit/  → BUILD_OK
go vet ./checkkit/    → VET_OK
go test ./checkkit/   → ok  github.com/opencharly/sdk/checkkit  0.004s
```
The committed tests exercise the new behavior: TestRunProvisionAct_NilExecutorMapsToFail (the act leg's nil-executor guard — fails without the leg) + the updated TestVerbResolverNilExecutor (the shared dispatch's guard) + the MCPProvide snapshot coverage in the consumer cutover's tests.

## Rulebook compliance
- R1 (RCA every anomaly): the nil-runner panic found by the committed test was RCA'd + guarded.
- R3 (one home, no duplication): the dispatch body exists exactly once (the shared helper); the pieces live in the SDK's one home.
- R4 (no workarounds): no sleeps/retries; the nil guards are the mechanism's own contract.
- R6 (git safety): PR-only, no force-push, no direct main.
- R7 (prove the gate): the pasted build/vet/test outputs above.
- N/A: R2 (no umbrella-level change), R5 (no legacy to delete), R8/R9 (no harness/CI change), R10 (the changed branch's live run is the consumer cutover's welded placement — the ceiling here is analysed on a live system, per the footer).

## Change classification
SDK addition; verification gate: the pasted outputs + the committed tests + the validator; attribution: analysed on a live system (the legs compile + the tests run live; the changed branch's live run is the consumer cutover's welded placement).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*