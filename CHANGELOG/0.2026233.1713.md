# 0.2026233.1713 — feat: adopt org-wide reusable auto-merge architecture

## feat: adopt org-wide reusable auto-merge architecture

### The change

Replaces the inline pr-validator with the dispatcher calling the
reusable workflow in `opencharly/.github`. Adds the
`approve-validator-runs` workflow for automatic approval of
GITHUB_TOKEN-created runs. Removes the old `auto-merge.yml`.

### Files

- `.github/workflows/pr-validator.yml` (modified) — dispatcher calling
  the reusable validator in opencharly/.github
- `.github/workflows/approve-validator-runs.yml` (added) — auto-approves
  action_required runs from GITHUB_TOKEN pushes
- `.github/workflows/tag-on-merge.yml` (existing) — tag dispatcher
- `.github/workflows/auto-merge.yml` (deleted) — old engine
- `CHANGELOG/2026.233.1703.md` (added)

### How tested

The architecture is proven on opencharly/.github (PRs #26, #27, #30,
#33, #34 — all merged automatically). This PR's landing through the
same flow is the live proof for this repo.

### Change class

CI/workflow — architecture adoption.

### Project-rulebook compliance

- **R2** — one coherent change.
- **R3** — no duplication: all logic in the shared .github repo.
- **R4** — no workaround.
- **R5** — old auto-merge.yml deleted.
- **R6** — no force-push.
- **R8** — N/A.
- **R9** — N/A.
- **R10** — the gate is this PR's landing through the validator.

#

## Auto-merge finalization

This run finalized the CHANGELOG at merge time: the placeholder was renamed to `CHANGELOG/2026.233.1707.md` (H1 rewritten to `# 2026.233.1707`). The current diff carries the finalized file.

<!-- auto-merge-finalization-note: 2026.233.1707 -->

## Attribution

*Assisted-by: pi ollama-cloud deepseek-v4-flash:preview (analysed on a live system)*