# 2026.210.2103 — buildkit DistroConfig/BuilderConfig → thin spec aliases (#55 step 3-III, sdk leg)

The consumer leg of #55 step 3-III. The `DistroConfig`/`BuilderConfig`
value-type containers + their resolution methods + the free funcs
`WrapDistroDef`/`DistroTagChain` are now sourced from the `spec` contract
(merged spec `v0.2026210.2052`); `sdk/buildkit` keeps only **thin type aliases**
so its build-engine code reads unchanged — the same alias pattern buildkit
already uses for `DistroDef`/`BuilderDef`/`BuilderMap`.

## What lands

- **buildkit/format_config.go** — the `DistroConfig`/`BuilderConfig` struct
  definitions + their methods + the `WrapDistroDef`/`DistroTagChain` free funcs
  DELETED (−285 lines); replaced by `type DistroConfig = spec.DistroConfig` /
  `type BuilderConfig = spec.BuilderConfig`. The intrinsic-lookup methods now live
  on the generated spec types (`spec/{distro,builder}_config_methods.go`).
- **buildkit/{config_resolve,resolved_box,system_packages}.go** — 3 stale comments
  that described these as "buildkit-specific types spec must never import" corrected
  (R1 doc divergence; the claim was inverted by the relocation, claim-keyed R5
  swept whole-tree).
- **go.mod / go.sum** — `require github.com/opencharly/spec` bumped
  `v0.2026210.1533` → `v0.2026210.2052`.

`InitConfig` is deliberately UNCHANGED (composition policy — `ResolveInitSystem`/
`ActiveInit` aggregate the whole candy set via `AggregateCandyCapabilities` —
relocates with the build engine, #65/#71).

## Verification

- `GOWORK=off go build ./...` + `go vet ./buildkit/...` + `go test ./buildkit/...`
  EXIT 0 against the real merged spec tag `v0.2026210.2052`; `gofmt` clean.
- Purely a re-home: buildkit's public surface is byte-identical (the aliases
  resolve to the same underlying spec types), so every downstream consumer builds
  unchanged.

Producer-first B2: spec (merged `v0.2026210.2052`) → this sdk leg → superproject
charly + 85-plugin repoint.
