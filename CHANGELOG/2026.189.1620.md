# 2026.189.1620 — sdk/spec: InstallStep interface (P4-unit-2)

Moves the InstallStep interface — the polymorphic element of the InstallPlan IR
E-envelope — out of charly into sdk/spec (`install_step.go`). It returns only spec
types (Kind()StepKind / Scope()Scope / Venue()Venue / RequiresGate()Gate /
Reverse()[]ReverseOp — all spec after P4-unit-1), and belongs with the InstallPlan
container that holds a []InstallStep (both destined for spec). Homed in spec (not
deploykit) because deploykit imports spec, so the interface must be the base: the 13
concrete step structs (the step VOCABULARY, sdk/deploykit later) implement
spec.InstallStep structurally. Second P4 unit.

*Assisted-by: Claude (fully tested and validated)*
