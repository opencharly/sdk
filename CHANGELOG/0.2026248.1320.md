# 0.2026248.1320 — loaderkit: cycle-break the PluginKinds wire walkers

## Summary

R1 regression fix: capturePluginKinds/restorePluginKinds (materialize_wire.go) recursed the Namespaces graph without a pointer-identity guard; a mutual-import cycle (main<->sub aliasing) looped forever. Caught by charly's TestImportNamespace_MutualCycle after the charly wave's sdk pin bump (the pre-bump loader never reached this materialize path on the cyclic tree). Same aliasing rule the project walk uses; the guard is also the materialize_wire.go simplification surface flagged in the parser consolidation.

## How tested

cd sdk && go build ./loaderkit/ && go test ./loaderkit/  (ok, 0.937s); charly's TestImportNamespace_MutualCycle: PASS 0.06s (was: infinite loop -> OOM-killed).

## Rulebook compliance

R1 (root-caused regression, fixed), R3 (one aliasing rule), boundary law (loader mechanism), R6 (PR-only).

## Change classification

Class: code change (loader mechanism fix). Gate: loader tests + the charly cycle e2e (pasted). Attribution: fully tested and validated (the exact failing path now passes with the fix, fresh run).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (fully tested and validated)*