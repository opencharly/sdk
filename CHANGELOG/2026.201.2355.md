# 2026.201.2355 — Cutover B-1 sdk-contract leg (PLACEHOLDER — re-stamped at merge)

**Placeholder CalVer.** The fresh `pr-validator` renames this file and
rewrites the heading to the merge-time stamp per the standard CalVer
mechanics; do not edit this file after it merges.

## What this leg is

The sdk-contract counterpart to the superproject's Cutover B-1 landing
(pod-lifecycle-dispatch full port + bundle-remainder + kernel-B addendum —
three independently-developed units assembled by the B-1 integrator).
Three disjoint sdk legs, merged clean with zero conflicts:

- **Unit 2 (pod-lifecycle + enc/secrets + vm-client SDD cleanup)**: adds
  `deploykit/enc_probe.go` + `secret_probe.go` (27 genuinely portable
  encrypted-volume and podman-secret helper functions relocated from
  charly-core) and `schema/vmclient.cue` (6 new CUE-sourced wire types:
  `#VmPluginEnv`, `#VmSnapInternalReq`, `#VmSnapshotCreateOpts`,
  `#VmSnapshotEntry`, `#VmDisplayEndpoint`, `#VmResolveResult` — closing an
  SDD violation where charly-core and candy/plugin-vm each hand-wrote their
  own twin of the same wire shape).
- **Unit 3+4 (bundle-remainder)**: removes the dead candy-level
  `libvirt?: [...]` field from `#Candy`/`#CandyModel` (schema/candy.cue,
  schema/candymodel.cue) + the matching `CandyReader.Libvirt()`/
  `HasLibvirt()` interface methods and implementer
  (spec/candy_reader.go, deploykit/spec_candy_adapter.go,
  loaderkit/scan_candy.go). `#SchemaVersion` bumped
  `2026.186.2323` → `2026.201.2300` (PLACEHOLDER — re-stamped again at THIS
  merge, per the standard CalVer mechanics; see the superproject leg for the
  matching migration-table entry).
- **Unit 5 (kernel-B addendum)**: adds `deploykit/deploy_forward.go` (k3s
  port-forward helpers, reusing `vmshared.SplitPortForward` per R3) and
  extends `deploykit/deploy_state.go` with `FindBundleNode`, moved from
  `charly/k3s_post.go`'s pure-function half.

## Verification (integrator's battery, on the merged tree)

- `task cue:gen`: regeneration is a **complete no-op** on the merged CUE
  sources — `spec/cue_types_gen.go`, `spec/vocab_gen.go`,
  `spec/version_gen.go` all reproduce byte-for-byte (the three units' hand
  auto-merge of `spec/cue_types_gen.go` during the git merge was NEVER
  trusted — regenerated fresh from the merged `schema/*.cue`, confirming
  reproducibility).
- `GOWORK=off go mod tidy && go mod verify && go build ./... && go test
  ./...`: clean/ok across all 16 packages (including
  `spec.TestGenReproducible`).
- `gofmt -l .`: clean.
- `golangci-lint run ./...` (`GOWORK=off`): 0 issues.

## R10 — acceptance roster (run against the superproject's frozen integrated
## tree, `charly` `fa4bd91` / this sdk gitlink)

Orchestrator-run, all concurrent, all GREEN, zero warnings:

- `check-sidecar-pod`: PASS (steps=12), exit 0 — run 2026.202.0005,
  check-live 2m46.9s with the full pod-lifecycle round-trip coverage
  (shell/logs/update/remove) under concurrent load.
- `check-exampledeploy`: PASS (steps=6), exit 0.
- `check-group`: PASS (steps=7), exit 0.
- `check-k3s-vm`: PASS (steps=8), exit 0 — exercises unit 5's
  `deploykit.FindBundleNode`/port-forward helpers live.
- Migration entry live-proven by direct probe: a real pre-sweep
  qemu-guest-agent fixture at schema `2026.186.2323` migrated via
  `applied strip-candy-libvirt-field (schema 2026.201.2300)` — the
  candy-level `libvirt:` block stripped, `charly box validate` clean,
  second run idempotent no-op.

## Merge mechanics

Three independent branches (`feat/cutover-b-pod-lifecycle`,
`feat/cutover-b-bundle-remainder`, `feat/cutover-b-kernelb`), each an
independent sdk-repo checkout (no shared git-dir — distinct linked
superproject worktrees each get their own sdk submodule clone), combined by
adding each as a local remote and `git merge --no-ff` in sequence onto a new
`feat/cutover-b1` branch. Zero conflicts — the three units' sdk-side file
sets are fully disjoint except `spec/cue_types_gen.go`/`spec/version_gen.go`
(generated; regenerated fresh post-merge per the rule above, never
hand-merged).

## Fix round (post-PR, pr-validator CHANGES REQUESTED)

The first validator pass on this PR (#82) FAILED with a real, verified
finding: `deploykit/secret_probe.go` shipped 11 relocated secret-leaf
functions, but only ONE (`GenerateRandomSecretToken`) had actually been
wired into the paired consumer (`charly#169`'s `enc.go`) — the other ten
were dead weight (uncalled anywhere, zero test coverage), while
byte-identical duplicates in `charly/secrets.go` remained the ONLY
implementation actually exercised. The package comment asserted the
relocation was complete; it wasn't (R1: a false claim in shipped code,
verified false against the paired consumer's real content). Simultaneously
R3 (the same functions duplicated across two repos) and R10 (zero test
coverage for the untested slice).

**RCA — how did "repointed every call site" survive to the PR review
undetected by the author's own battery:** the enc-side relocation
(`enc_probe.go`) genuinely WAS fully wired — `enc.go` calls
`deploykit.EncPlanFor`/`FuseAllowOtherEnabled`/etc. live, with the old
in-file copies deleted. That success masked the secrets-side gap: `go
build`/`go test -count=1` on BOTH repos passed green the whole time,
because Go's compiler and test runner only prove that code COMPILES and
that EXISTING tests pass — neither one can detect "this new exported
function is never called by anything" or "this old private function is
still live when a newer public twin exists elsewhere." Nothing in the
author's verification battery (build, vet, test, gofmt, lint, cue:gen
no-op) is designed to catch a dual-implementation duplication where BOTH
copies independently compile and pass their own tests. Only the fresh
`pr-validator`'s cross-repo call-site grep against the PAIRED consumer's
actual content caught it — this is precisely why B2's "the sdk PR's own
evidence and claims depend on the paired consumer" cross-PR check exists,
and why R10's coverage gate ("a change whose new functionality has no test
that would fail without it is not landable") is enforced independently of
a green build/test run.

**Fix**: `secret_probe.go`'s package comment corrected to describe the
now-true state (every call site verified repointed, zero old-name
occurrences left in `charly/secrets.go` per the R5 grep self-test — see
the paired consumer PR for the actual repoint). Added
`deploykit/secret_probe_test.go` (13 tests) covering every one of the 11
functions, including the 4 that shell out to `podman`/`docker` — those are
tested via a per-test empty-`$PATH` override (`t.Setenv`) so
`exec.Command`'s `LookPath` deterministically fails regardless of whether
the CI/dev host has a live podman daemon, proving the error-handling
contract WITHOUT ever risking a real host-state mutation (writing an actual
podman secret from a plain `go test` run would itself be exactly the class
of accidental host-config side effect this project's disposable-only
discipline forbids outside a `disposable: true` bed).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; fully tested and validated)
