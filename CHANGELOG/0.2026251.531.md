# 0.2026251.531 — F2: candy/cue/doc-stream restated on the parsed node (loaderkit); bridge tests moved beside the implementation

## Summary

Parser-consolidation F2 wave (plan/nested-capture-architecture.md, Cutover F2 — the genericNode dual representation dies) — the **sdk slice**: the three mechanisms restated on the wire-safe spec.ParsedNode, beside the implementation, with the bridge test coverage moved in.

1. **`loaderkit.CandyIsImage` / `loaderkit.BuildCandy` (candy_node.go)** — the candy box⊻layer routing restated on the parsed node: CandyIsImage scans the canonical JSON body (`pn.Body`) for the `base`/`from` marker (the deleted genericNode yaml-discValue key scan); BuildCandy decodes through the SAME shared CUE entity decoder (`DecodeNodeValue`) every other candy/kind decode goes through — so the deleted round-trip machine's byte-parity guarantee (pn→gn→pn lossless) is now STRUCTURAL: the parsed-node constructor IS the canonical decode.
2. **`loaderkit.CueDocFromJSON` (cue_validate.go)** — the canonical-JSON CUE ingest (cuejson.Extract + the loader's own cueSchemaCtx; the JSON twin of CueDocFromYAML). The validateKindValueCUE gate's deleted gn→yaml.Marshal→CueDocFromYAML round trip becomes a direct JSON ingest.
3. **`loaderkit.ParseDocStream` (parse_doc_stream.go)** — the ONE per-document pipeline (kit.ClassifyDoc → #NodeDoc GateDoc → registered spec.DocParser → deterministic directive serialization → import/discover collection). `walk.parseDocs` now calls the composer; the binary-embedded default-vocabulary stream (charly's materializeDocStream) drives the SAME composer through the loader seam — the hand-rolled second copy dies in the charly slice.
4. **Moved tests** — the five bridge test files' candy/child-form coverage moves beside the implementation: `candy_node_test.go` (CandyIsImage scan cases + the candy round-trip proof from node_candy_test.go, rewritten against parsed-node fixtures), `node_childform_test.go` (the compact node-form grammar from node_childform_test.go — all three rejection error strings live in loaderkit/parse.go), and `parse_doc_stream_test.go` (the composer's contract: stream order, empty-doc skip, gate propagation, import/discover collection).

## How tested

All commands on the final committed tree (branch `feat/f2-bridge-deletion`), `-count=1`:

```
$ gofmt -l .            # empty (nothing printed)
$ go build ./...        # exit 0
$ go vet ./...          # exit 0
$ go test ./... -count=1
ok  github.com/opencharly/sdk           0.115s
ok  github.com/opencharly/sdk/loaderkit 0.552s
ok  github.com/opencharly/sdk/kit       2.526s
... (10 packages, all ok) ...
```

The new/moved coverage (parsed-node candy + child-form + composer):

```
$ go test ./loaderkit/ -run "TestCandyIsImage|TestBuildCandy|TestChildForm|TestParseDocStream" -count=1
ok  github.com/opencharly/sdk/loaderkit  0.020s
```

## Rulebook compliance

- R3 — CandyIsImage/BuildCandy/CueDocFromJSON/ParseDocStream are the ONE parsed-node implementations; the walk's parseDocs is now a thin caller of the composer (the same loop, no second copy).
- R5 — additive here: the bridge files (node_parsed.go/node_candy.go/node_build.go) and their charly-side callers die in the charly slice of the same wave; the moved tests are rewritten against the parsed node, never keeping the old bridge fixtures.
- R6 — branch-only (feat/f2-bridge-deletion), no force-push, no hook bypass. R10 — all gates on the final committed tree.

## Change classification

- Change class: refactor + relocated-test wave (parser-consolidation F2)
- Verification gate: gofmt/build/vet + full sdk test suite green on the final committed tree
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*