# 2026.203.1027 — dead-code radical-removal batch (sdk leg)

## Removed

Proven-dead exported/unexported symbols deleted with zero-call-site proof (grep
across `charly/`, every `sdk/*` package, and all 83 `candy/plugin-*` modules; the
`golangci-lint` `unused` linter was clean across the whole tree already, since it
does not flag exported symbols in library packages — these were found by manual
cross-module reference-count sweep instead):

- **`vmshared.ValidateLibvirtSnippet`** (`vmshared/vm_helpers.go`) — exercised only
  by its own now-deleted test (`charly/libvirt_test.go`'s `TestValidateLibvirtSnippet`);
  zero production call site anywhere. The one place a caller was *documented*
  (`plugins/internals/skills/libvirt-renderer/SKILL.md`, claiming invocation from a
  `validateLibvirt` rule in `candy/plugin-box/validate_rules.go`) turned out itself to
  be stale — that rule does not exist in the current tree — an R1 documentation-
  divergence incident fixed in the same commit as the plugins leg of this batch.
  `IsDeviceElement` in the same file is a genuine sibling (real production caller via
  `candy/plugin-vm/libvirt.go`'s `isDeviceElement` alias) and was kept.
- **`kit.FirstNonEmpty`** (`kit/migrate_helpers.go`) — zero call sites anywhere,
  including its own tests.
- **`kit.CheckLevels`** (`kit/checklevel.go`) — a `var` declared "for error
  messages" that no error message (or anything else) ever read. The rest of the
  check-level ladder (`ResolveCheckLevel`, `IsValidCheckLevel`, `CheckLevelReaches`,
  `checkLevelRank`) is unaffected and confirmed live.
- **`deploykit.DropMappingKey`** (`deploykit/deploy_state.go`) — zero call sites.
- **`vmshared.ISOBuilderAvailable`** (`vmshared/cloud_init_iso.go`) — zero call
  sites; its doc comment's claim ("Used by `charly doctor`...") does not match any
  code in the current tree.
- **`vmshared.MirrorSnapshotsToDeployState`** (`vmshared/vm_snapshot.go`) — zero call
  sites; the `spec.VmSnapshotState` type it constructed is independently live via
  `spec/hand_state_types.go`'s `vm_state.snapshot` field and was NOT touched.
- **`spec.PackageItemsFromStrings`** (`spec/union_types.go`) — zero call sites,
  including tests; its doc comment's claimed migrator caller does not exist. Its
  sibling `PackageNames` (real caller) was kept.
- **`vmshared.SnapshotsRefcountSummary`** (`vmshared/vm_snapshot.go`) — zero call
  sites.
- **`deploykit.ResolveDeployBoxName` / `resolveDeployKeyToBoxFromProject`**
  (`deploykit/deploy_box_name.go`, whole file, 49 lines, deleted as an appended
  commit after team-lead ruling) — zero call sites anywhere and zero test
  coverage, despite a header comment citing a K-wave label ("the PORTABLE half
  of the deploy-key → image-name resolver (K4)") and describing itself as
  written for "a plugin, or any future consumer" that never materialized. An
  annotation citing a future K-wave use does not make uncalled code migration
  inventory — it makes it dead code with an aspiration; the K4 wave can
  re-create it from git history the day a real caller exists.

Each deletion's now-unused import was removed in the same edit (`errors` from
`vm_helpers.go`, `strings` from `vm_snapshot.go`); no other import became stale.

## Proof

- `go build ./...`, `go vet ./...`, `golangci-lint run ./vmshared/... ./kit/...
  ./deploykit/... ./spec/...` (0 issues), `go test ./...` — all clean on the final
  tree (pasted in the PR body).
- R5 grep self-test: every deleted identifier greps clean outside this repo's
  `CHANGELOG/`.

## Correction: the "no bed run" call above was OVERRULED

A fresh pr-validator pass correctly rejected the anti-waste argument above:
the R10 gate-by-change-class matrix has exactly ONE bed-exempt class
(documentation-only), and the anti-waste principle sizes the bed ROSTER
(which beds), it never waives the bed itself. Ran two disposable beds on a
fresh rebuild instead — see `opencharly/charly#179`'s CHANGELOG entry and PR
body for the full verdicts (both `PASS`, exit 0, zero residue); this leg's
changes are exercised transitively through the sdk module that binary was
built from. Confidence tier re-stamped `analysed on a live system`
accordingly.
