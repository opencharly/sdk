# 2026.195.0606 — #CheckBedReply.image_tag: the per-run bed-scoped image tag field

Additive optional field on the check-bed session-seam reply (`#CheckBedReply`, right
after `bed_domain`) so the host can thread a per-RUN bed-scoped image tag
(`<bed-root>-<runCalver>`) to the compiled-in bed-runner plugin. Every
`charly box build` + deploy in a bed run passes it as `--tag`, so concurrent beds
building the SAME fixture image name never collide on the store-global
short-name→newest-local-CalVer resolution (superproject #75 — the tag analogue of
`bed_domain`=deploy-name). NOT an authored config surface (a host↔plugin seam wire
type), so no `#SchemaVersion` bump. Generated via `task cue:gen`
(`spec/cue_types_gen.go` `CheckBedReply.ImageTag`); the reproducibility gate stays
green.

*Assisted-by: Claude (fully tested and validated)*
