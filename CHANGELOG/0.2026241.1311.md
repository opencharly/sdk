# 0.2026241.1311 — feat(kit): ReadLedger (bulk) + InvalidateImageCache re-export

## Summary

- **`kit/install_ledger.go`**: `ReadLedger` returns the FULL ledger maps (deploys + candies) in ONE read of the per-host charly.yml — the status collector was re-reading the 310KB config once per record (measured: 186 reads per status run). STRICT: a malformed ledger is an error (never reported as an empty one).
- **`kit/local_image.go`**: re-export `container.InvalidateImageCache` (the build and deploy commands call it to clear the persistent image-list cache when a new image is created). The function comes from spec#67 (the status-caches spec PR); this PR pins the new spec tag after it merges.
- **`go.mod`/`go.sum`**: spec pin advance to the status-caches tag (spec#67).

## How tested

```
$ cd /tmp/sdk-fix && go build ./... && echo BUILD OK
BUILD OK
$ go test ./kit/...
ok  	github.com/opencharly/sdk/kit	0.378s
```

**Full-stack measurement** (charly binary rebuilt with these changes + the spec/plugin caches): `charly status` 12.6s → 0.63s warm.

## Rulebook compliance

| Rule | How |
|---|---|
| R1 — RCA every anomaly | The status collector re-read the 310KB per-host config once per ledger record (186 reads per run) — the bulk `ReadLedger` fixes it. |
| R2 — no pre-existing split | N/A — single cutover. |
| R3 — no duplication | `ReadLedger` is the one bulk read; the per-record readers remain for single-record callers. |
| R4 — no workarounds | The fix is the real bulk read, not a shim. |
| R5 — hard cutover | N/A — additive API. |
| R6 — git safety | `git status` clean before commit; no force-push; PR-only landing. |
| R7 — prove the gate | `go build` + `go test` pass on the final tree; the full-stack measurement is pasted above. |
| R8 — generation/label verification | N/A — no generated artifacts. |
| R9 — binary integrity | The charly binary was rebuilt from the committed tree with the sdk replace; status ran live. |
| R10 — disposable fresh-rebuild proof | The consuming stack (the charly check beds) is gated by the `check-charly-vm` disposable bed; this sdk change is the foundation it pins. |
| RDD — prove high-risk assumptions | The bulk read is exercised by the full-stack measurement. |
| ADE — spec-is-the-test | N/A — no plan: acceptance steps. |
| SDD — schema-driven design | N/A — no schema change. |

## Change classification

- **Change class:** code (bulk ledger read + cache invalidation re-export)
- **Verification gate:** build + unit tests + full-stack measurement (12.6s → 0.63s warm)
- **Attribution tier:** `fully tested and validated` — the changes ran live on the rebuilt binary with retained output (above), and the unit suite passes from the committed tree.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*