# 0.2026253.1947 — feat(loaderkit): the deploy-hop from: resolves namespace-qualified templates

## Summary

DeployTargetEntity's deploy-hop resolved the bed's from: against the LOCAL vm map only — a bed cloning from a git-linked namespace golden (from: omarchy.omarchy-vm) failed the hop ("no kind:vm entity") even though the qualified template resolves via ResolveKindEntityBody. The hop now falls back to the canonical namespace-aware body lookup (ResolveKindEntityBody) — the same surface the vm-build entity resolution uses.

## How tested (pasted output)

### Unit tests

```
$ go build ./loaderkit/
(clean)

$ go test ./loaderkit/ -run "TestDeployTargetEntity_QualifiedFromHop" -v
=== RUN   TestDeployTargetEntity_QualifiedFromHop
--- PASS: TestDeployTargetEntity_QualifiedFromHop (0.00s)
PASS
ok  	github.com/opencharly/sdk/loaderkit	0.003s

$ go test ./loaderkit/ -run "TestDeployTargetEntity|TestResolveEntityRef"
ok  	github.com/opencharly/sdk/loaderkit	0.005s
```

### R10 — fresh-rebuild disposable bed proof (the deploy-hop, live)

The changed code path (the deploy-hop's qualified-from: fallback) was executed against a FRESH rebuild of charly with this sdk change (go.mod replace to the PR head) on a disposable VM bed. The bed's deploy carries a NAMESPACE-QUALIFIED from: chain — the acceptance bed's from: names the base bed (check-omarchy-eval-edge-inst), whose own from: is the git-linked namespace template omarchy.omarchy-vm — the exact case the hop previously failed.

BEFORE (charly built against the pre-change sdk — the hop fails to resolve the qualified from:):

```
$ charly vm build check-omarchy-eval-edge-inst --from-snapshot golden
charly: error: command "vm": VM "check-omarchy-eval-edge-inst" has no kind:vm entity in vm.yml.
                 For a bootc VM, declare one in vm.yml:
                     vm:
                       check-omarchy-eval-edge-inst-bootc:
                         source:
                           kind: bootc
                           image: check-omarchy-eval-edge-inst
                         disk_size: 20G
```

AFTER (fresh rebuild with this change — the hop resolves the qualified from: chain and the clone build succeeds):

```
$ /tmp/charly-sdk248 vm build check-omarchy-eval-edge-inst --from-snapshot golden
xorriso : UPDATE :       2 files added in 1 seconds
ISO image produced: 185 sectors
Written to medium : 185 sectors at LBA 0
Writing to 'stdio:output/qcow2/check-omarchy-eval-edge-inst/seed.iso' completed successfully.

Wrote output/qcow2/check-omarchy-eval-edge-inst/disk.qcow2 (clone of check-omarchy-eval-edge-inst@golden)
Wrote output/qcow2/check-omarchy-eval-edge-inst/seed.iso
Wrote VM box localhost/charly-check-omarchy-eval-edge-inst:2026.253.1943
EXIT=0
```

The BEFORE/AFTER pair is the proof: the pre-change hop cannot resolve the qualified from: at all ("no kind:vm entity"); the fresh rebuild resolves the chain (check-omarchy-eval-edge-inst → omarchy.omarchy-vm via ResolveKindEntityBody) and the clone build completes. The bed is disposable (disposable: true); the golden was provisioned by the base bed's own R10 run.

## Rulebook compliance

- R0 (skills first): the charly-vm:vm + charly-check:check skills govern this change — N/A to the diff itself
- R1 (RCA): the bed vm-build failure was RCA'd (the deploy-hop's local-only from: resolution) before the fix
- R2 (no duplication): ResolveKindEntityBody is the single namespace-aware lookup
- R3 (one implementation): the hop uses the same surface as the vm-build entity resolution
- R4 (no workarounds): the fix is the correct resolution
- R5 (delete legacy): the local-only hop lookup is retired
- R6 (git safety): PR-only landing
- R8 (no dead code): N/A
- R9 (no magic numbers): N/A
- R10 (fresh disposable proof): the fresh-rebuild bed run above (the deploy-hop with a namespace-qualified from: chain)

## Change classification

- Change class: feature (the deploy-hop resolution)
- Verification gate: unit tests + fresh-rebuild disposable bed run
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*