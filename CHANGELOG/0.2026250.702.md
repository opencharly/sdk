# 0.2026250.702 — chore: re-pin spec to v0.2026250.656 — consume the #111+#114 tag (CC-2 sdk follow-up)

## Summary

CC-2 (sdk consumers of the spec IR rename) follow-up: the sdk's fleet-vocabulary
retirement PR (#229, tag v0.2026250.527) pinned spec at v0.2026250.523 — the
#111 tag. spec #114 (fix: comments keep the current sdk/cli names until their
rename PRs land, validator B5) landed AFTER that pin; the NEWEST spec tag
carrying BOTH #111 and #114 is **v0.2026250.656**.

This PR bumps go.mod `github.com/opencharly/spec v0.2026250.632 → v0.2026250.656`
(sdk main had already moved to .632 via #230's #Packaging consumption), so the
sdk rides the full #111+#114 spec state.

spec #114 is comment-only (schema/seam.cue + spec/cue_types_gen.go — no type or
method surface change), so no sdk code change is required beyond the pin.

## How tested

From the final committed tree (`2449d2b`, worktree clean):

```
$ go build ./... ; echo BUILD_EXIT=$?
BUILD_EXIT=0

$ go vet ./... ; echo VET_EXIT=$?
VET_EXIT=0

$ go test ./... -count=1
ok  github.com/opencharly/sdk       0.096s
ok  github.com/opencharly/sdk/agentkit    0.349s
ok  github.com/opencharly/sdk/buildkit    0.007s
ok  github.com/opencharly/sdk/candywalk   0.004s
ok  github.com/opencharly/sdk/deploykit   3.034s
ok  github.com/opencharly/sdk/enginekit   0.006s
ok  github.com/opencharly/sdk/kit     2.351s
ok  github.com/opencharly/sdk/loaderkit   0.522s
ok  github.com/opencharly/sdk/packagekit  0.020s
ok  github.com/opencharly/sdk/vmshared    0.117s
TEST_EXIT=0
```

Claim-keyed sweep (executed on the pushed tree, sdk main already carries the
#229 rename):

```
$ git grep -inw -E 'LoadFleetConfig|SaveFleetConfig|FleetConfig|FleetNode|flattenFleetVenues|FleetTargetForDisc|BuildFleetNode|BuildFleetNodeInto|acc\.Fleet' -- ':!CHANGELOG*'
SWEEP1_EXIT=1 (zero matches) LINES=0

$ git grep -iw fleet -- ':!CHANGELOG*' ':!go.sum'
SWEEP2: 41 lines, ALL 41 are candy/plugin-fleet REPO-name references
(non-plugin-fleet matches: 0) — dispositioned: the plugin rename rides a later PR.
```

## Rulebook compliance (R0-R10, dispositioned)

- **R0 (Skills first)** — git-workflow skill loaded before any git/gh action; umbrella + charly AGENTS.md read first.
- **R1 (RCA every anomaly)** — the stale-pin gap (sdk on .523/.632 while newest #111+#114 tag is .656) was RCA'd by mapping spec tags to PRs (v0.2026250.523=#111, .632=#112, .656=#114). No gate failures occurred.
- **R2 (Finish the whole cutover)** — the fleet vocabulary is fully retired inside the sdk (sweep 1: zero identifier matches). Remaining non-CHANGELOG `fleet` matches are exclusively `candy/plugin-fleet` repo-name references, which ride the plugin-repo rename in a later PR (org-level, outside sdk scope).
- **R3 (No duplication)** — no new code; one canonical pin updated in go.mod.
- **R4 (No workarounds)** — no sleeps/retries/fallbacks; the pin moves to the legitimately newest #111+#114 tag.
- **R4a (Fix the product first)** — N/A: no product defect; spec #114's comment revert is consumed as the spec's own final state.
- **R5 (Delete legacy completely)** — the legacy vocabulary was deleted by the already-merged #229; go.sum retains the old version's module hashes (normal Go module-graph metadata, not a legacy path).
- **R6 (Git safety)** — status inspected; branch cut from current origin/main; no force-push, no direct push to main; PR-only landing; hooks untouched.
- **R7 (Prove behavior, not compilation)** — full test suite -count=1 on the committed tree (10/10 ok) against spec .656 + the executed claim-keyed sweep.
- **R8 (Preserve emitted artifacts)** — N/A: library pin bump emits no labels/plans/configs.
- **R9 (Binary equals source)** — N/A for the sdk contract module (Go library, no worktree binary; the module tag at merge is the deliverable).
- **R10 (Fresh disposable proof)** — all gates run from the final committed tree (2449d2b), clean worktree, -count=1.

## Change classification

- **Change class:** chore — dependency pin bump (CC-2 follow-up; spec v0.2026250.632 → v0.2026250.656)
- **Verification gate:** go build + go vet + go test -count=1 on the committed tree, plus the claim-keyed vocabulary sweep
- **Attribution tier:** fully tested and validated — the module compiled and all 10 packages' tests executed live against spec v0.2026250.656

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*