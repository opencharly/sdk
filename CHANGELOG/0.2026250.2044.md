# 0.2026250.2044 — refactor(loaderkit): F1 parser consolidation — ONE resolver envelope, member walk, deploy-shape classifier, body transform

## Summary

Parser-consolidation F1 wave (Cutover F1, plan/nested-capture-architecture.md) — sdk/loaderkit slice. ONE resolver envelope, ONE member walk, ONE deploy-shape classifier, ONE body transform.

1. **F1.1 — ONE substrate/resource OpResolve envelope.** The marshal→`InvokeProvider(kind:<word>, OpResolve)`→decode dance was implemented inline 7 times: `Resolve{Resource,Android,Vm}ViaExecutor` (resolve_via_executor.go), `Resolve{Vm,Kubernetes,Android}EntityViaExecutor` (resolve_kind_entity_via_executor.go), and `refs_seams_executor.go`'s `resolveLocalViaPeer`. New `resolve_substrate_executor.go` holds the generic `resolveSubstrateViaExecutor(ctx, ex, word, input)` + `decodeResolveReply[T]`; every typed wrapper is now a one-liner. Behavior-identical (same typed request envelopes, same reply-decode contract, same empty-reply handling).

2. **F1.2 — `ResourceChildren` deleted.** Its only caller was the charly host's deploy-shape composite, subsumed by `IsDeployShape`'s member channel; the loaderkit impl and the spec seam method (opencharly/spec#122, merged, tagged v0.2026250.2036) die together with the plugin-loader delegator (opencharly/plugin-loader#7) and the charly host change (charly PR, next in wave). The ONE member walk stays: `BuildResourceMemberChildren` over the ONE `memberDisc`/`IsResourceDisc` predicate. The frozen `#ResourceKind`-only filter also MISSED structural/external members; the member channel checks the authored children themselves, so those classify now too.

3. **F1.3 — parse.go's local `mapValue` deleted.** Identical to `kit.MappingChild` (the ONE copy); the 3 call sites use `kit.MappingChild`.

4. **F1.4 — `IsDeployShape` is the ONE deploy-shape classifier.** Arms: member children, scalar cross-ref, from:/image: mapping, and the `agent_provisioned: true` imageless spelling (the Deploy gate's box exemption — lockstep per the 2026-09-07 RCA).

5. **F1.5 — `EntityBodyJSON` returns `pn.Body` directly.** `pn.Body` IS the parse-time output of the same yaml→map→JSON transform; the reconstruction-then-retransform round-trip is gone. The member-carrier strip (Cutover C task 0) is preserved in pure JSON — byte-identical, locked by `TestParse_MemberCarrierKeysStrippedFromBody`.

Plus: new unit test `TestIsDeployShape_Arms` locking all four arms; pre-existing gofmt alignment fix in packagekit/render_test.go (repo CI gofmt gate checks the whole tree).

## How tested

All commands on the final committed tree (spec pinned v0.2026250.2036), `-count=1`:

```
$ go build ./... && go vet ./...    # both exit 0
$ go test ./...                     # exit 0 — all 10 packages ok (sdk, agentkit, buildkit, candywalk, deploykit, enginekit, kit, loaderkit, packagekit, vmshared)
ok  github.com/opencharly/sdk/loaderkit 1.9s (incl. TestParse_MemberCarrierKeysStrippedFromBody, TestIsDeployShape_Arms)

$ go test ./loaderkit/ -run TestIsDeployShape_Arms -count=1 -v
=== RUN   TestIsDeployShape_Arms
--- PASS: TestIsDeployShape_Arms (0.01s)
PASS
ok  github.com/opencharly/sdk/loaderkit 0.016s
```

**FAIL-RED/PASS-GREEN (the charly-side fold proof, pasted — NOT described):** the consolidated classifier runs live inside charly's kernel (compiled-in loader), in the charly F1 branch. First, the `agent_provisioned` arm is TEMPORARILY stripped from the consolidated `IsDeployShape` (uncommitted mutation, documented as the fail-red state) — the fixed-fold suite:

```
$ go test . -run 'TestSubstrateKind' -count=1   # charly module, FAIL-RED state
--- FAIL: TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy (0.00s)
    substrate_imageless_deploy_test.go:61: imageless agent_provisioned pod DROPPED from acc.Deploy (deploy keys []); pod template map: [agent-live]
FAIL
FAIL	github.com/opencharly/charly/charly	1.028s
```

(The 2026-09-07 RCA bug — the imageless agent_provisioned pod silently classified as a standalone TEMPLATE — reproduces through the NEW architecture when the arm is missing: the fixed-fold test is the guard.)

Restored to the committed state (`git diff` empty against HEAD), the FULL fixed-fold suite passes:

```
$ go test . -run 'TestSubstrateKind' -count=1 -v
=== RUN   TestSubstrateKind_BothShapesByteEquivalent
--- PASS: TestSubstrateKind_BothShapesByteEquivalent (0.02s)
=== RUN   TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedPodFoldsToDeploy (0.00s)
=== RUN   TestSubstrateKind_PlainImagelessPodStaysTemplate
--- PASS: TestSubstrateKind_PlainImagelessPodStaysTemplate (0.00s)
=== RUN   TestSubstrateKind_ImagelessAgentProvisionedWithSiblingStillFolds
--- PASS: TestSubstrateKind_ImagelessAgentProvisionedWithSiblingStillFolds (0.00s)
PASS
ok  github.com/opencharly/charly/charly 0.512s
```

The full charly kernel suite also ran green (112.7s, `go test . -count=1`) on the same tree.

**B14(d) sweep — no `ResourceChildren` residue (real commands + output):**

```
$ cd sdk && git grep -n 'ResourceChildren' -- '*.go' | grep -v node_tree.go
[exit 0 — zero matches outside node_tree.go's deletion comment]
$ grep -rn 'ResourceChildren' charly/charly --include='*.go' | grep -v _test.go | grep -v node_tree.go
[exit 1 — zero matches: the host call is gone; only the historical comment remains]
```

## Rulebook compliance

- R3 — one canonical implementation each: resolve envelope (1), member classification (1: `memberDisc`/IsResourceDisc), member walk (1: `BuildResourceMemberChildren`), deploy-shape classifier (1: `IsDeployShape`), yaml mapping lookup (1: `kit.MappingChild`), body serializer (1: parse-time `entityBodyJSON` → `pn.Body`).
- R5 — `ResourceChildren` and parse.go's `mapValue` deleted in the same wave as their last callers (spec#122 merged; plugin-loader#7 + charly PR in wave).
- R1 — spec's `TestGenReproducible` failure root-caused (hand-edited generated mirror in #121) and fixed at the source (CUE `{[string]: string}` spelling + regen) in spec#122; the `map[string]string` wire form is now tool-emitted, byte-identical.
- Boundary law — `IsDeployShape` stays kind-blind and data-derived; no kind-word switch.
- R6 — branch-only, no force-push, hooks untouched. R10 — all gates on the final committed tree; fail-red state was an explicit temporary mutation, restored byte-perfect before the green run.

## Change classification

- Change class: code/config refactor (Go dedup + relocation, spec dependency pin bump, gofmt fix)
- Verification gate: build/vet/full test suite green on the final committed tree + live charly-side fold proof pasted (fail-red/pass-green)
- Attribution tier: fully tested and validated — the changed runtime paths (the ONE classifier + the fold) executed live in charly's kernel suite with pasted output, and the fixed-fold guard was proven fail-red/pass-green

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*