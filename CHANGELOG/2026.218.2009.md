# 2026.218.2009 — CollectSkills + the ai.opencharly.skill label + sdk/candywalk

The sdk leg of the plugins->candies migration.

- `CollectSkills` (deploykit) projects the composed candies' authored skill entities into the
  `ai.opencharly.skill` OCI label — an image is self-describing (skills readable via
  `charly box labels`, no external fetch). The CapabilityLabelMap entry + WriteLabels emission +
  render_prep wiring make the label live, superseding the dead doc-pointer URL.
- The discovered-candy parse (`ParseCandyManifest`) accepts STACKED manifests — a candy node plus
  sibling skill:/hook:/marketplace: entities.
- New `sdk/candywalk` kit: the shared entity-discovery walk (superproject + box submodules) that
  plugin-docs and plugin-marketplace both use (R3 — no second copy).
- The `charly box new candy` scaffold teaches the new skill:/hook:/marketplace: surface.
