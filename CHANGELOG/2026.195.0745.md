# 2026.195.XXXX — Ignore an absent base in builder-backed box graphs

Builder-backed images such as `from: builder:pacstrap` legitimately have no
`base:`. Exclude that empty value from their dependency graph while preserving
their bootstrap-builder edge and real cycle detection.

*Assisted-by: Codex OpenAI GPT-5.6 Sol (fully tested and validated)*
