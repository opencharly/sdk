# 0.2026249.1056 — feat(loaderkit): the bed validator accepts the from: name:tag deploy-hop

## Summary

The **from: name:tag deploy-hop** (Phase 3): a check bed's from: may name ANOTHER check bed (the clone-base bed) whose own from: names the template — the loader now accepts it via uf.CheckBeds() membership (the template map remains the primary check), and the build drive resolves the chain at build time.

## How tested (LIVE, pasted)

The hop's accept path EXECUTED LIVE in the converted instrument bed (disposable: true target, fresh-rebuild binary with the hop):

```
$ charly check run check-instrument-cachyos-vm     # run 2026.249.1032 — steps ran to vm-create
[vm-build] PASS — log:
  Building VM "check-vm-clone-base" (source.kind=clone)
  Wrote output/qcow2/check-vm-clone-base/disk.qcow2 (clone of check-vm-clone-base@golden)
```

The vm-build step drove `vm build check-vm-clone-base --from-snapshot golden` — the deploy-hop ACCEPTED the from: naming the base bed (the bed validator's hop) + the drive resolved the chain + wrote the clone. The full-tree validate on the same tree: 505 candies, 40 boxes, 0 errors.

```
$ go test ./loaderkit/ -run TestValidateCheckBedsAcceptsDeployHop -v
--- PASS (the accept path; an undefined from: is still rejected)
```

## Rulebook compliance

- R0: git-workflow + strict-policy loaded.
- R1: pasted live output; the reject/accept boundary unit-gated.
- R2: same named batch (Phase 3); the loader split (#217), the schema (#99/#100/#101), the drive (#24/#25), the runner (#26) are the other legs.
- R3: the hop uses the ONE chain resolver (DeployTargetEntity — sdk #219) — no duplicated walk.
- R4: no workarounds.
- R5: additive — the retirement stays plugin-vm's.
- R10: the unit gate + the live bed run above executed the changed accept path.

## Change classification

Class: validator (additive). Gate: unit gate + the live converted-bed run (pasted above). Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*