# 0.2026254.523 — chore: bump spec to v0.2026254.503 (the docs-config contract)

## Summary

Single dependency bump: opencharly/spec `v0.2026250.2036 → v0.2026254.503` — the merged docs-config contract (closed #DocsConfig schema + generated spec.DocsConfig types + ClassifyDoc classification for the top-level docs: node). No code changes. This is the first consumer of the new contract so the loader (loaderkit consumes spec.ClassifyDoc / the #NodeDoc gate) can route a docs: node from a charly.yml.

## How tested

From the pushed tree (branch feat/spec-docs-config @ eb7702068d330cc8c54ca29f5472ccd987d64299):

- `go build ./...` exit 0.
- `go test ./...` exit 0 — all 11 packages ok (sdk, agentkit, buildkit, candywalk, checkkit, deploykit, enginekit, kit, loaderkit, packagekit, vmshared).
- go.mod diff = 1 line (spec requirement); go.sum +2 lines; nothing else changed.

## Rulebook compliance

- Single-purpose dependency bump; no behavior change beyond the contract surface.
- PR-only landing via the org validator; no direct-main push, no force-push.

## Change classification

- **Change class:** dependency pin (contract module).
- **Verification gate:** build + full module tests on the committed tree.
- **Attribution tier:** analysed on a live system — build/tests executed live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*