# 2026.195.9999 — #67 render α-fold — the Containerfile render DRIVE moves into sdk/deploykit

#67 (the render-half of the landed P8 move): the Containerfile render DRIVE — `Generate` /
`generateContainerfile` / `generateDataImageContainerfile` — is homed in `sdk/deploykit`
(`deploykit.Generator`), driven by `candy/plugin-build` over the resolved-project envelope. The host
box-build render-leg is deleted; render is byte-identical to the pre-move core render (proven by the
superproject's golden Containerfile diff + the `check-pod` R10 bed).

## sdk/deploykit (the render DRIVE + adapters)

- `deploykit/generate.go` (NEW) — `Generate` / `generateContainerfile` / `generateDataImageContainerfile`:
  a pure orchestrator over PRE-FILLED build-render caches on `*buildkit.ResolvedBox`
  (RenderCandyOrder/CandyCaps/ActiveInits/InitSystem/InitDef/BakedMetadata) + CandyModel + the host-coupled
  SEAM fields on `Generator`. NO live `*Candy`/`*Config` reads.
- `deploykit/write_labels.go` (NEW) — `WriteLabels` (the formatter half of the split `writeLabels`): emits
  the OCI LABEL lines from a `*spec.BakedLabelSet` byte-for-byte.
- `deploykit/render_seam.go` (NEW) — the per-method param/result structs for the `HostBuild("render-seam")`
  dispatch (RenderService, builder resolves, ValidateEgress, EmitPluginOp, localpkg, header-copy,
  ensure-builders).
- `deploykit/spec_candy_adapter.go` + `spec_resolved_box_adapter.go` (NEW) — the envelope→render adapters
  (`NewSpecCandyModel` / `NewSpecResolvedBox`); `HasContent` / `HasInstallFiles` read the host-precomputed
  CandyModel booleans (the live `*Candy` verdicts — the envelope cannot recompute them faithfully).
- `deploykit/render_generator.go` — added the `EmitBakedPlugins`, `CollectBoxVolume`, `ValidateTextEgress`
  seams; `ValidateEgress` carries a `Mode` (bytes vs text vs xml).

## sdk/spec + schema (SDD)

- `schema/buildresolve.cue` — `#RenderSeamRequest`/`#Reply` (method + opaque `bytes`) +
  `#BakePluginsRequest`/`#Reply`; `#BuildResolveBox.containerfile` removed;
  `#BuildResolveReply.resolved_project` added.
- `schema/candymodel.cue` — `has_content` / `has_install_files` (the host-precomputed predicates).
- `spec/cue_types_gen.go` — regenerated (`task cue:gen`); reproducible (a second run is a no-op).
- `spec/label_consts.go` (NEW) — the `ai.opencharly.*` label key constants (the single source the
  deploykit WriteLabels EMITTER + the ExtractMetadata deploy READER share).
- `buildkit/resolved_box.go` — the build-render cache fields (BakedMetadata/RenderCandyOrder/InitSystem/
  InitDef/ActiveInits/CandyCaps).

## Test

- `deploykit/render_parity_test.go` (NEW) — a committed byte-golden for the moved render DRIVE (a minimal
  envelope fixture → `deploykit.Generator.Generate` → golden Containerfile), guarding the move against
  regressions. The full box-build byte-parity is proven by the superproject's origin/main-vs-new diff + the
  `check-pod` R10 bed.
