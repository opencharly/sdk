# 2026.233.1339 — Use clean auto-merge engine from .github

Replaces the pull_request:labeled dispatcher with the clean
workflow_run-based dispatcher that calls the reusable engine in
opencharly/.github. The engine renames the CHANGELOG placeholder,
dispatches a validator re-run, and enables native auto-merge.

Change class: CI/workflow — dispatcher update.
