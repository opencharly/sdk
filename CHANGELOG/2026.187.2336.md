# Sweep the spice_preresolve.go comment ref (R5, H part 2)

vmshared/ssh_tunnel.go's header still named the deleted host `spice_preresolve.go` as a
consumer. The spice/vnc check verbs' host-side graphics-endpoint resolution moved to charly's
`resolveVerbGraphics` (the cc.ResolveGraphicsEndpoint reverse-leg); reword the comment. Zero
behaviour change.
