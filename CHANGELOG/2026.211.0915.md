# 2026.211.0915 — the sdk leg of #55 step1-fabric (charly core off every sdk mechanism kit)

The `opencharly/sdk` leg of #55 step1-fabric. Relocates the contract + fabric-primitive
surface out of the `sdk` mechanism kits (`kit`/`deploykit`/`buildkit`/`loaderkit`/`vmshared`)
into the `opencharly/spec` contract module (the spec leg, PR #10), and re-exports each
relocated symbol from its kit so every candy call site compiles UNCHANGED. The end state:
**`charly/` core imports ZERO sdk mechanism kits** — `kit=0`, `deploykit=0`, `loaderkit=0`,
`buildkit=0`, `vmshared=0` (`git grep -E 'github.com/opencharly/sdk/(kit|deploykit|buildkit|loaderkit|vmshared)' charly/` prod non-test → EMPTY). The kits remain shared CAPABILITY libraries consumed
by PLUGINS (never charly core), now backed by `spec` slices.

## What lands (thematic arcs)

- **Granular spec slices consumed**: the kits re-export the fabric primitives the spec leg
  relocated — `spec/exec` (host/guest executors + VenueDescriptor re-materialization, the sole
  `x/crypto/ssh` containment boundary), `spec/transport` (go-plugin/grpc dial+serve),
  `spec/lock` (Flock), `spec/container` (podman/docker host-engine probes, the local-image
  ref-resolution family, TransferImage), `spec/hostenv` (runtime-config + ReexecOverSSH +
  host-env primitives), `spec/spec` (universal wire/IR value types, the QuadletDir family,
  builder-map value-primitives, cache-mount render, BuilderResolveInputFrom), `spec/refs`
  (git/refs prims). Each kit keeps a `var X = specslice.X` re-export so plugin consumers are
  byte-stable; the spec slice is the single source (R3), the kit body is DELETED.
- **kit → 0 in charly/**: the `kit` grab-bag split — fabric primitives (executor slice,
  runtime_config, container engine helpers, lock, value/naming, git/refs, QuadletDir,
  ReexecOverSSH, FirstYAMLVersionLine, check host-vantage/CheckContext/InputStr) re-export
  their spec slices; the check-Runner/deploy-config/builder capability half moves with the
  deploy/check/build cones. `charly/` prod `kit` importers 19→0 (coneG main.go + refs.go,
  coneD bundle_add_cmd, coneC C1/C3 host_build_config_resolve + host_build_pod_config_seams,
  coneB Class A, coneF loader_discover, check-engine cone).
- **deploykit → 0 in charly/** (the dominant axis): the deploy walk / bundle tree / state /
  secrets / pod-config / box-resolve clusters relocate plugin-side behind the pre-existing
  `deploy*`/`podConfig*`/`step-emit`/`configResolve`/`buildengine-*`/`check-*` seams, OR
  self-resolve via the cfg-load keystone (`loaderkit.LoadUnified` is plugin-reachable over the
  K1 reverse leg — NO new `loader-config` seam needed). `charly/` prod `deploykit` importers
  21→0 across coneB2 (#12 generate.go), coneK1 (remote_image.go 15→14 FIRST count shed, #8
  box-ref-resolve seam-death, resolved_project_host K1 loader-cone fabric-tail),
  coneK1c (check_endpoint_resolve 14→13), coneWalk (plugin_executor_reverse.go 13→12,
  ctx-threading via `sdk.ContextWithHostStepDeps`), coneSpecBuilder (refs.go 12→11),
  coneB-br2 (build_overlay+enc+layer_secrets+host_build_box_ref_resolve 6→2, the α secret
  cluster plugin-side + build.DistroCfg DROP + cfg.ResolveBoxRef), coneC-dsh (DeployStateHost
  reader-callback signatures), coneC C2 (LoadBundleConfigViaExecutor seam-death), and
  coneK3tasks (charly/tasks.go — the LAST prod importer; BuilderResolveInputFrom + the
  cache-mount render cluster relocated to `spec/spec` as pure value-assembly over spec types).
- **loaderkit → 0 in charly/**: `coneA` Q2(1) — `check_cmd.go`'s `resolveMergedDeployTree` shed
  via context-threading (`spec.ProjectLoader.ResolveMergedDeployTree` retrieves
  `sdk.ExecutorFromContext(ctx)`; the plugin runs `loaderkit.ResolveMergedTreeViaExecutor`).
  Plus the shared `LoadUnifiedViaExecutor` + `ResolveMergedTreeViaExecutor` plugin-side helpers.
- **buildkit / vmshared → 0 in charly/**: the render/resolve cluster moves plugin-side
  (coneSpecBuilder builder-map, coneK3tasks cache-mount render + BuilderResolveInputFrom);
  vmshared value-types stay in `spec/spec`, re-exported from `vmshared`.
- **K5 regression fix**: `loaderkit.LoadBundleConfigViaExecutor` (the cycle-free plugin-side
  replacement for the deleted `deploykit.LoadBundleConfigViaSeam`) returned `(nil, nil)` for
  an absent/empty per-host overlay; its drop-in predecessor `deploykit.LoadBundleConfig`
  returns a NON-NIL `&BundleConfig{}`. The `check-commands-local` `reap-orphans-runs` step
  regressed (the `if dc == nil` guard short-circuited the scan). Restored to
  `&deploykit.BundleConfig{}` (non-nil) on absent/empty, matching the pre-K5 contract for all
  10 K5-rewired callers; the false `(nil,nil)`-on-absent comments corrected (R1 claim-keyed).
- **Check-run reply wire types — SDD sweep (this leg)**: the spec leg CUE-sourced
  `StepResult`/`CheckRunReply`/`StepPass` (DELETING `spec/spec/checkresult_wire.go`) and
  narrowed the P12 gengotypes exception to EXACTLY `kit.CheckResult`'s engine-internal
  `DeadlineExceeded bool json:"-"`. This leg sweeps the sibling `sdk/kit` comments
  (`checkrun_seam.go` + `checkresult.go`) that still claimed the reply types were the
  "hand-written wire-mandate exception" — now accurate: both request AND reply are
  CUE-sourced in `spec/schema/checkresult.cue`, generated into `spec/spec/cue_types_gen.go`
  (byte-identical, `optional=nillable` emits the Passthrough/Score pointers); the exception is
  the `kit.CheckResult` wrapper's `json:"-"` flag only.
- **go.mod re-point (this leg)**: `require github.com/opencharly/spec` bumped
  `v0.2026211.754` → `v0.2026213.1726` (the spec leg's merged tag), + `go mod tidy`.
- **R1 claim-keyed comment sweeps**: the coneB-br2 sibling-set (7 `sdk/deploykit` comments
  made present-tense-false by the `enc.go`/`layer_secrets.go` deletion), the coneK1load
  namespaced-box resolve fold, and the coneA `bake-plugins`/`resolved-project→InvokeProvider`
  sweeps — all comment-only, keeping the sdk tree's docs honest against the relocated code.

## Why

`charly/` keeps ONLY the E/M/B/D plugin-host fabric; every capability is a plugin candy. The
mechanism kits are shared CAPABILITY libraries consumed by PLUGINS, never charly core. This
leg (B2 leg 2 of 4: spec → sdk → plugins → super) re-points `sdk/go.mod` at the spec leg's
merged tag and re-exports every relocated symbol, so the downstream super leg can re-point
`charly/` and the candies off the kits onto the spec slices — the end state
`git grep 'github.com/opencharly/sdk/(kit|deploykit|buildkit|loaderkit|vmshared)' charly/` is
EMPTY (verified: 0 prod `charly/` files import any sdk mechanism kit after the full #55
accumulation).

## R10 proof

The sdk leg is a Go contract/library module; its own gate (run this turn, GOWORK=off) is GREEN:
`go build ./...` ok; `go test ./...` ok; `golangci-lint run ./...` 0 issues; `gofmt -l .` empty.
The runtime R10 roster — the disposable check beds that build against the ASSEMBLED step1-fabric
tree — runs at the SUPER leg of this B2 chain (the super leg re-points the sdk gitlink to this
leg's merged commit and re-runs the fresh-rebuild roster). The full #55 accumulation R10 roster
was GREEN driven from the assembled tree: check-pod PASS, check-sidecar-pod PASS,
check-substrate PASS, check-exampledeploy PASS, check-commands-local PASS (the K5 regression fix
re-verified here — `reap-orphans-runs` PASS), check-charly-vm, check-builder-vm, check-structkind,
check-group, check-local, check-doctor-local, check-feature-local, check-udev-local,
check-gpu-local, check-preempt-local, check-migrate-local all PASS; the K5 `check-commands-local`
regression reproduced pre-fix → PASS post-fix on the rebuilt binary.

## B2 dependency

This is leg 2 of the 4-leg B2 chain (spec → sdk → plugins → super). The spec leg (#10, merged
`v0.2026213.1726`) produced the relocated contract + fabric slices this leg re-exports; the super
leg re-points the sdk gitlink to this leg's merged commit and re-runs the fresh-rebuild roster.

Assisted-by: Claude Code glm-5.2:cloud (fully tested and validated)