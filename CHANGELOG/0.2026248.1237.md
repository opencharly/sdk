# 0.2026248.1237 — checkcontext: sdkCheckContext.InvokeProvider over the single dial

## Summary

Implements the new `spec.CheckContext.InvokeProvider` (spec#97, the one-dial doctrine): session/service verbs submit provider invocations over the SAME single-broker connection a check verb already dialed - a second Dial hangs per sdk's own doctrine (checkverb.go header). This push adds the nil-exec guard (clean error instead of a nil dereference), the B12 guard tests, and the gofmt import-ordering fix.

## How tested (fresh + live)

```
$ cd sdk && gofmt -w checkverb.go && go build ./... && go test -count=1 ./...
ok  github.com/opencharly/sdk
ok  github.com/opencharly/sdk/agentkit
ok  github.com/opencharly/sdk/buildkit
(… full suite green …)

$ go test -count=1 -run TestCheckContextInvokeProvider -v ./...
--- PASS: TestCheckContextInvokeProvider (0.00s)
```

**LIVE (the changed path executed on a real run - the wave's one-dial E2E probe):** the plan-step `spice: session` START now passes live THROUGH `cc.InvokeProvider` (the session dispatch into the runner's `verb:session` service):

```
PASS  check plan-level SPICE SESSION start (E2E reverse-leg submission)  [spice-sess-start]  session sess1 started
```

plus the detached recorder lifecycle probe against the VM (systemd spawn -> 599 KB real frames -> systemctl stop -> FINAL + row.json). The one-dial path is what unblocked it (the prior second-dial hang is gone).

## Rulebook compliance

- **R1**: the second-dial hang was RCA'd to the one-dial doctrine; this PR is the fix's sdk half.
- **R3**: one accessor (invoke over the existing exec); no duplicated dial paths.
- **R5**: N/A - no removals.
- **R8/R9**: `gofmt -w` clean (B18 fixed); generated wire untouched.
- **R10/RDD**: the changed path executed live in the wave E2E probe (pasted); ADE/SDD N/A for a contract impl.
- **Boundary law**: surface only; no new ops/classes.
- **R6 / git-workflow**: PR-only; no force-push.

## Change classification

- **Class**: code change (contract implementation).
- **Verification gate**: fresh gofmt/build/test + the live one-dial probe (changed path executed).
- **Attribution**: analysed on a live system (fresh gates; the changed dispatch path ran live in the wave E2E).

*Assisted-by: pi ollama/deepseek-v4-flash:0731 (analysed on a live system)*