# 0.2026266.2340 — docs(sdk): sweep the stale spec/cache.Store comment in load_cache.go

## Summary

Sweep the stale `spec/cache.Store` wording in `loaderkit/load_cache.go`'s STORAGE comment. It still described the DELETED keyed store ("a directory of one JSON entry per key"), while the code already uses the merged `spec/cache` **ArtifactStore** (`cache.OpenLayoutLimited` / `cache.StoreDir` / `Fill`). The comment diverged from reality (R1/R5).

Comment-only; no behavior change.

## How tested

**R5 sweep — no stale `spec/cache.Store` reference survives in live code:**

```
$ grep -rn "spec/cache\.Store\|cache\.Store\b" --include=*.go --include=*.md . | grep -v CHANGELOG/
(no output — exit 1)
```

(`cache.StoreDir(...)` is the CURRENT ArtifactStore API — a false positive for this sweep, left in place.)

**Gates, real output:**

```
$ gofmt -l loaderkit/load_cache.go
(no output — exit 0)
$ go build ./...
(no output — exit 0)
$ go test -count=1 ./loaderkit/
ok  	github.com/opencharly/sdk/loaderkit	2.623s
```

## Rulebook compliance

- **R0** — loaded `/charly-internals:git-workflow`, `/charly-internals:go`.
- **R1** — the comment-vs-code divergence is the incident; fixed here.
- **R2** — the whole sweep for this file is here.
- **R3** — the wording names the ONE ArtifactStore mechanism; no second description.
- **R4** — no workaround.
- **R5** — the stale identifier is gone (grep pasted).
- **R6** — `fix/sdk-stale-comment` off `origin/main`; no force-push.
- **R7 / R7a** — comment-only; the module suite is the gate.
- **R8 / R9** — no label/runtime change.
- **R10** — documentation-only change class: the module suite + the R5 grep self-test.

## Change classification

- **Change class:** Documentation-only (comment-only code edit) — `loaderkit/load_cache.go`.
- **Verification gate:** the non-runtime standards (R5 grep self-test + `gofmt`/`build`/`test`).
- **Attribution tier:** `documentation reviewed`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*