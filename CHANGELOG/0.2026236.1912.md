# 0.2026236.1912 — test(buildkit): pac host cell renders repo setup (regression for check-cachyos-vm)

## Summary

The pac `phase.install.host` cell in `charly/charly.yml` now imports the candy's keys and appends its repos to `/etc/pacman.conf` before `pacman -Syu` (charly#401). This test pins that render with a literal host-cell fixture, mirroring `TestRpmHostCellHandlesRepos`. The charly-side presence check lives in `charly/format_config_test.go` (charly/ core must not import sdk).

## How tested

```
$ go test ./buildkit/ -run "TestPacHostCellHandlesRepos|TestRpmHostCellHandlesRepos" -count=1
ok  	github.com/opencharly/sdk/buildkit	0.003s
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | RCA: the check-cachyos-vm `target not found: charly` traced to the pac host cell having no repo handling; this test locks the fix |
| R2 | No deferral |
| R3 | Fixture mirrors the charly.yml host cell (one vocabulary) |
| R4 | N/A — test-only |
| R5 | N/A |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Test output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | Tests ran on the committed tree at this head |

## Change classification

- **Change class**: test (regression pin for the pac host cell repo render)
- **Verification gate**: the render test + the org validator
- **Attribution tier**: `fully tested and validated` — the changed test ran green on the final tree

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*