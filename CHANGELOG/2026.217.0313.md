# 2026.217.0313

The sdk leg of the K-wave seam-death terminus (everything-is-a-plugin 100%): 29 commits — the
loaderkit growth that makes plugins load projects themselves, the deploykit concurrency-correctness
fixes the terminus roster surfaced, and the wave-era relocations.

## loaderkit (W1/K1 — the program keystone)

The loader orchestration relocates out of charly core: kind-decode support, entity-body assembly,
the bundle tree-builder, the box-validate entity-tree walk, remote-repo fetch orchestration +
candy-ref collection, and the `Resolve{Vm,K8s,Android}EntityViaExecutor` helpers. The canonical
`LoaderExecutor` witness receipt lands (K3-W2). Terminus fixes: a shadowed remote candy ref now
resolves to the LOCAL body (the deterministic plugin-source fix — shadowing is effective, not
advisory); remote-ref collection seeds from requested namespaced boxes (#17);
`RunBootstrapPhase` fails loudly instead of silently falling back (#20).

## deploykit (W3/W4 + the terminus concurrency arc)

- `BringUpMembers`/`TearDownMembers` land at their canonical home, and `BringUpMembers` now ends
  with a precise cross-member container-DNS preflight (derived from the ${HOST:member} refs the
  bed actually declares; a resolution failure is an INFRA-class error naming the stale-aardvark
  condition and its recovery).
- ONE locked read-modify-write cycle (`MutateBundleConfig`) for the per-host deploy overlay —
  every writer routes through the flock + re-read + merge-on-latest cycle; the three per-candy
  lock-helper copies are deleted (the run-2 lost-update family: clobbered overlay refs,
  resurrected preempt claims, vanished entries).
- Build temps are held against the stale-temp sweep (flock-held roots), and a check bed with a
  missing package source now fails loudly (`dev_local_pkg` threading) instead of silently
  skipping the rpm build.
- The render-service seam moves onto InvokeProvider (B4).

## kit

The false "`${DEPLOY_NAME}` addresses your own cluster" claim is corrected across the check-vars
docs (#18 — DEPLOY_NAME resolves the shared VM entity, not the deploy, for VM live checks).

Proof: the 28-bed disposable roster at measured caps (run 3 + the three-bed re-acceptance),
red/green unit tests per fix (concurrency cycle, shadow preference, sweep-hold, DNS preflight),
zero warnings. Breaking changes carry `!` per commit.
