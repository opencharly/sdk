# 2026.218.0000 — K-wave 2: the box_engine caller-list correction

The sdk docs follow-up of the K-wave 2 residue-ledger program: deploykit/box_engine.go's
header named deleted charly-core files as callers of ResolveBoxEngineForDeploy. The
comment now names the real candy consumers (plugin-deploy-pod, plugin-pod,
plugin-preempt, plugin-substrate) and the actual ResolveBoxEngine home
(sdk/deploykit/box_build_resolve.go). Placeholder CalVer — the pr-validator finalizes
it at merge.
