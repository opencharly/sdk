# Remove the dangling DataKeys comment in schemagen

Documentation-class (comment-only). `internal/schemagen/main.go` carried a
4-line comment describing a `DataKeys` vocabulary set that was removed in the
schema-compaction cutover — `vocabSets`/`renderVocab` emit no `DataKeys`, and
`spec.DataKeys` does not exist. The dangling comment is deleted; no code change.

Part of the plugin-first standing-order cutover (the R1 claim-keyed sweep of the
removed `DataKeys` identifier); the superproject CHANGELOG entry of the same
CalVer carries the full narrative.
