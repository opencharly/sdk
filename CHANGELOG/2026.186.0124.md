# M15 support: kit.DefaultDeployConfigPath + kit.MappingRoot

Added kit.DefaultDeployConfigPath() + kit.DeployConfigEnv (the per-host deploy-overlay path
resolver, deployconfig.go) and kit.MappingRoot (the YAML document→mapping unwrap, migrate_helpers.go)
so charly core (via aliases) and the out-of-module candy/plugin-migrate share ONE definition of
each (R3) when the migration engine moves out of charly core. Reworded the OpBootstrap doc comment
(ops.go): it is a generic pre-validation transform hook, NOT the migration path — config-schema
migration is candy/plugin-migrate's command:migrate over OpRun, never a raw-byte bootstrap transform.
