# 2026.211.0130 — deploykit InstallStep vocabulary → thin spec re-exports (#55 step-4, sdk leg)

The consumer leg of #55 step-4. The InstallStep step vocabulary — the 13 concrete
step structs + `RepoSpec`/`CacheMountSpec`/`ArtifactRef` + their methods + the view
converters + plan mechanisms — is now sourced from the `spec` contract (merged spec
`v0.2026210.2308`); `sdk/deploykit` keeps only **thin re-export aliases** so its own
code + out-of-tree plugins are unchanged.

## What lands

- **deploykit/steps.go** — the 13 struct definitions + helper types + methods
  DELETED, replaced by `OpStep = spec.OpStep` / `FileStep = spec.FileStep` / …
  (the same alias pattern deploykit already uses for the IR envelope
  InstallPlan/InstallStep/EmitOpts/…). ~1330 LOC removed across steps.go /
  step_view.go / plan.go / plugin_step_words.go.
- **deploykit/step_view.go, plan.go, plugin_step_words.go** — the view converters
  (`StepToView`/`StepFromView`/…) + plan mechanisms (`WireView`/`PlanFromView`/…)
  now re-export the spec implementations.
- **deploykit/step_view_test.go** — DELETED (the `TestStepView_RoundTrip` gate
  relocated to spec/install_step_view_test.go).
- **go.mod / go.sum** — `require github.com/opencharly/spec` bumped
  `v0.2026210.2052` → `v0.2026210.2308`.

The InstallStep vocabulary is IN-PROC IR (never crosses the wire — `StepToView`
projects it onto the CUE-sourced `InstallStepView`), so it relocates hand-written
beside the already-hand-written `InstallStep` interface + `InstallPlan`/`StepBatch`
in spec; the wire form is untouched. `sdk/deploykit`'s public surface is
byte-identical (the aliases resolve to the same spec types), so every downstream
consumer builds unchanged.

## Verification

- `GOWORK=off go build ./...` + `go test ./deploykit/...` EXIT 0 against the real
  merged spec tag `v0.2026210.2308`; `gofmt` clean.

Producer-first B2: spec (merged `v0.2026210.2308`) → this sdk leg → superproject
charly + in-tree-candy repoint.
