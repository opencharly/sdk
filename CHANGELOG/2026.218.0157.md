# 2026.218.0157 — K-wave 2: the residue-ledger sdk leg

The sdk leg of the K-wave 2 residue-ledger program: the shared kit/loaderkit
readers and the wire-catalog growth the relocated seams consume. The program
(super CHANGELOG 2026.218.0000) drove charly/ core to the kernel/plugin boundary
law's end state, measured by the per-file residue ledger (56/56 satisfied,
tools/ledger-validate PASS).

- loaderkit: the ONE shared DocParser adapter; ResolveRetentionDefaultsViaExecutor
  (the shared keep-defaults resolve); ResolveVmStateViaExecutor + VmStateFromBundleConfig
  (the shared persisted-VmState read — the former VmJSON round-trip was identity);
  CanonicalRef (the import-ref fetch seam); the CUE-schema ownership.
- kit: ContainerExists (the shared container-existence probe); the dead
  plugin_primary duplicate deleted.
- The render-seam HostBuild deletion + builder connect via InvokeProviderOpts.ExtraRef.
- The parse_candy_manifest / project_candies / refs_seams_executor relocations.
- spec_box_resolve + ValidateEntityCUE.
- KongSubcommands includes hidden fields (Hidden:true) — the hidden-but-reachable
  run-local catalog; BuildCapabilities marshals it.

All wire types CUE-sourced (task cue:gen / wire:gen); regeneration on a clean tree
is a no-op.
